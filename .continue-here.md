# Continue Here — mc

**Created:** 2026-07-28
**Session focus:** 카테고리 전체 E2E 검증 + _parse_derivation 안정성 개선

---

## Current Position

카테고리 10종 체계 구축 완료. 파서 안정성 개선 완료. Phase 26 리팩토링 계획 대기.

---

## Completed This Session

1. **_parse_derivation() 안정성 개선 (chain_deriver.py)**
   - BOM(\ufeff), 제로폭 문자(\u200b~\u200f, \u2028~\u202f) 자동 제거
   - JSON 앞뒤 텍스트 삽입 대응 (fallback: 첫 `[`~마지막 `]` + `\[\s*\{` 패턴 매칭)
   - dict 응답에서 인식된 키(chain/posts/articles/items)만 추출
   - `json.loads` 실패 시에만 fallback 실행 (dict 응답은 fallback 안 탐)
   - 디버깅용 로그: 파싱 전략 실패 시 앞 200자 출력

2. **단위 테스트 12건 추가 (TestParseDerivationRobustness)**
   - BOM prefix, zero-width space, text before/after/around JSON
   - code fence (json 태그 포함/미포함)
   - dict with posts key → 추출, dict with topics key → 빈 리스트
   - empty input, no-json text

3. **카테고리 E2E 검증 결과**
   - ✅ customer_service (#234) — 소개→연결법(ARS우회/채널)→내선·후기
   - ✅ gov_finance (#235) — 개요→신청·계산기→FAQ·홈택스 배출
   - ✅ shopping_brand (#236) — 브랜드비교→가격·구매팁→후기·공식몰 배출
   - ✅ golf_course (#231) — 코스·위치→예약·요금→주변·후기·공식예약
   - ✅ medicine (#230) — 약·증상 개요→효능·복용→부작용·전문가 상담

4. **pytest: 297 → 309 통과** (신규 12건 + 기존 회귀 0건)

---

## Remaining Work

### Phase 26 — 코드베이스 리팩토링 (계획 수립 완료, 미착수)
- `.planning/phase-26/PLAN.md` 참고
- Wave 1: 공통 유틸리티 추출 (frontmatter_utils, url_utils, constants)
- Wave 2: 카드 주입 시스템 모듈화
- Wave 3: 파이프라인 아키텍처 + 설정 검증

### 기타 잔존
- Persona 어조 설계 (config/personas.yaml)
- Shortcode git 미추적 (rotcha/techpawz)
- Chain #28 rotcha 복구 (````json` 제거)
- (a) 40건 고아 이미지

---

## Decisions

| 의사결정 | 내용 |
|----------|------|
| 파서 fallback 범위 | dict 응답은 인식된 키만 추출, topics 등 비인식 키는 fallback 안 탐 |
| 파서 BOM 처리 | `_parse_derivation` 내에서 strip + regex로 제거 |
| 카테고리 검증 범위 | 신규 6개(customer_service/gov_finance/shopping_brand/golf_course/medicine) + 기존 3개(travel/real_estate/stock) = 9개. automotive 제외 (검색 무효) |

---

## Environment Notes

- **Branch:** `feat/keyword-category-template`
- **DB Path:** `/Users/twinssn/Projects/5000/data/mc_chains.db`
- **pytest:** 309/309
- **Python:** 3.14.5

---

## Resume Instructions

```bash
cd /Users/twinssn/projects2/mc
python3 -m pytest --tb=short -q  # 309/309 확인

# 리팩토링 시작:
# .planning/phase-26/PLAN.md 참고
# 또는 기존 카테고리 운영:
# mc "새키워드"  # full pipeline
```
