# .continue-here.md — mc (Manual Chain)

**Updated:** 2026-07-24
**Phase:** Phase 17 CTA 코드화 (design → code)
**Status:** ✅ 코드 변경 완료, pytest 246/246, 배포 미실시

---

## Current Position

- **활성 Phase**: Phase 17 CTA 설계의 코드 구현 완료.
- **다음 단계**: Hugo 사이트 배포 (rotcha-blog, issue-techpawz-hugo, techpawz-hugo의 dual-cta.html 변경 반영) + 실제 발행 시 D8/D9 게이트 동작 확인.
- **기반 커밋**: `f24891a Phase 19: 규격 완전 적용 — Pollinations 제거, Unsplash/Pexels로 전면 교체`

---

## Completed This Session

### Wave 1 — `config/prompts.yaml`: `cta_phrases` 필드 추가

- 5개 카테고리(travel, real_estate, automotive, stock, etc) 각각에 `cta_phrases` 노드 추가:
  - `next_post`: "더 알아보기 →"
  - `entry_funnel`: "{{ENTRY_LINK}}"
  - `fallback`: "관련 주제 보기 →"
- `draft_user` 프롬프트의 CTA GUIDELINES 블록은 이미 존재 확인 (라인 228).

### Wave 2 — `chain_card_injector.py`: CTA 문구 통일 + DualCTA conv CTA 폐기

- `get_cta()` 기본값 변경: "계속 읽기 →" → "더 알아보기 →"
- `DualCTAInjector` 클래스 변경:
  - `info_cta_text` 기본값: "관련 글 모두 보기 →" → "이 시리즈 보기 →"
  - `conv_cta_text`, `conv_cta_url` 초기화를 `""`로 고정 (애드센스 모델에서 목적지 없음)
  - `build_dual_cta_html()` 출력에서 conv 관련 파라미터를 빈 문자열로 변경
- `config/chain_config.yaml`의 `card_cta`는 이미 "더 알아보기 →"로 통일되어 있음 확인.

### Wave 3 — `chain_publisher_core.py`: D8 CTA 릭 스캐너 확장

- 기존 CTA leak 패턴에 신규 CTA 텍스트 4개 추가:
  - `이 시리즈 보기`, `전체 글 모아보기`, `모아보기`
- 플레이스홀더 검출(`{{...}}`) 및 자동 제거 로직은 기존 유지.

### Wave 4 — Hugo shortcodes 업데이트 (mc repo 외부)

- 3개 Hugo 사이트의 `layouts/shortcodes/dual-cta.html` 변경:
  - 전환성 CTA 섹션 완전 제거
  - info CTA 버튼 스타일: `bg-red-600` + 흰 글씨 (CTA-SCENARIO.md §3.1 반영)
  - 기본 CTA 텍스트: "더 알아보기 →"
- 대상:
  - `/Users/twinssn/Projects/rotcha-blog/layouts/shortcodes/dual-cta.html`
  - `/Users/twinssn/Projects/issue-techpawz-hugo/layouts/shortcodes/dual-cta.html`
  - `/Users/twinssn/Projects/techpawz-hugo/layouts/shortcodes/dual-cta.html`
- `chain-card.html` shortcode는 이미 `#DC2626` 배경 + 흰 글씨 + "더 알아보기 →" 기본값이므로 변경 없음.

---

## Remaining Work

1. **Hugo 사이트 배포** (미완료)
   - rotcha-blog: `hugo --gc --minify` + `wrangler pages deploy`
   - issue-techpawz-hugo: 동일
   - techpawz-hugo: 동일
   - `layouts/shortcodes/dual-cta.html` 변경이 disk에만 존재, git에 커밋 필요

2. **hub_url 저장 로직** (별도 과제로 이월)
   - `loop_chains.hub_url` 3건 NULL.
   - `{{ENTRY_LINK}}` 치환 소스가 없어 현재 entry_funnel은 실제 발행 시 치환되지 않음.

3. **get_cta() category 분기** (미도입, 현재 불필요)
   - 전 카테고리 CTA가 "더 알아보기 →"로 통일되어 category 파라미터 불필요.
   - 추후 카테고리별 CTA 분기 재도입 시 `get_cta(direction)` → `get_cta(blog_key, direction, category)` 검토.

4. **cta_phrases 런타임 연동** (미완료)
   - `prompts.yaml`의 `cta_phrases`는 현재 코드에서 직접 읽히지 않고 참고용으로 저장됨.
   - 추후 `chain_deriver.py` 또는 발행 시점에 cta_phrases를 읽어 카드/듀얼 CTA에 반영하는 로직 추가 필요.

---

## Decisions

- **CTA 문구 통일**: 전 카테고리 `next_post` = "더 알아보기 →" (카테고리별 차이 없음).
- **DualCTA conv CTA 완전 폐기**: 애드센스 모델에서 제휴 목적지가 없으므로 info CTA만 유지.
- **플레이스홀더**: `{{ENTRY_LINK}}`만 유지, `{{FUNNEL_LINK}}` 폐기 유지.
- **D8 릭 패턴 확장**: 신규 CTA 텍스트("이 시리즈 보기", "전체 글 모아보기", "모아보기")를 자동 제거 대상에 추가.
- **Shortcode 스타일**: chain-card + dual-cta 모두 빨간 배경(`#DC2626` / `bg-red-600`) + 흰 글씨로 통일.

---

## Blockers

- **없음** — 코드 변경 완료, 테스트 통과.
- 단, hub_url NULL로 인해 `{{ENTRY_LINK}}` 치환이 현재 불가능함 (별도 과제).

---

## Bugs & Issues Discovered

### 1. DualCTA conv CTA가 빈 값으로 렌더링되던 문제 (이번 세션에서 수정)

- **증상**: `conv_cta_url = ""` → `#`로 대체되어 dead link가 생성됨. `build_dual_cta_html()`이 conv_url이 빈 값이어도 "#"로 fallback함.
- **원인**: 애드센스 모델 전환 후 제휴 목적지가 사라졌으나, `DualCTAInjector`는 여전히 conv CTA를 생성하려 함.
- **해결책**:
  - `DualCTAInjector.__init__`에서 `conv_cta_text`, `conv_cta_url`을 `""`로 고정.
  - `build_dual_cta_html()`에서 conv 관련 파라미터를 빈 문자열로 출력.
  - `dual-cta.html` shortcode 3개를 info CTA only로 재작성 (conv 섹션 제거).
- **미해결 여부**: 해결됨.

### 2. {{FUNNEL_LINK}} 플레이스홀더가 코드 어디에도 구현되지 않은 문제 (CTA-SCENARIO.md §6.2에서 확인)

- **증상**: CTA-SCENARIO.md 설계 당시 `{{FUNNEL_LINK}}`가 코드에 남아있을 수 있다고 의심했으나, 실제로는 코드 어디에도 없음.
- **원인**: 애드센스 모델 전환 시 제휴 로직이 함께 제거되었으나 플레이스홀더 개념만 문서에 잔존.
- **해결책**: 설계에서 `{{FUNNEL_LINK}}` 폐기 결정. 코드 변경 없음 (이미 없음).
- **미해결 여부**: 해결됨 (설계 결정).

---

## Environment Notes

| 항목 | 값 |
|------|-----|
| OS | macOS (darwin) |
| Python | 3.14.5 |
| Runtime | opencode |
| Working dir | `/Users/twinssn/projects2/mc` |
| Hugo sites | `/Users/twinssn/Projects/{rotcha-blog, issue-techpawz-hugo, techpawz-hugo}` |
| Chain DB | `/Users/twinssn/Projects/5000/data/mc_chains.db` |
| Shared themes | `/Users/twinssn/Projects/shared-themes` |
| 배포 시 env | `env -u CLOUDFLARE_API_TOKEN wrangler pages deploy` |
| Hugo 빌드 | `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify` |

---

## Resume Instructions

```bash
cd /Users/twinssn/projects2/mc

# 1. 상태 확인
python -m pytest --tb=short -q   # 246/246 기대
git status --short

# 2. mc repo 변경사항 커밋/푸시 (아직 미커밋)
git add config/prompts.yaml chain_card_injector.py chain_publisher_core.py
git commit -m "feat: Phase 17 CTA 코드화 — cta_phrases 추가, CTA 통일, DualCTA conv 폐기, D8 릭 패턴 확장"
git push

# 3. Hugo shortcode 변경사항 커밋/배포 (3개 사이트)
for site in rotcha-blog issue-techpawz-hugo techpawz-hugo; do
  cd /Users/twinssn/Projects/$site
  git add layouts/shortcodes/dual-cta.html
  git commit -m "feat: dual-cta shortcode — conv CTA 폐기, 빨간 버튼 스타일 적용"
  git push
  HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify
  env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./public --project-name $site
done

# 4. 실제 발행 테스트로 D8/D9 게이트 동작 확인
#    - 체인 발행 시 [D8-GATE] 로그 출력 확인
#    - 카드 주입 시 기존 chain-card/dual-cta 정상 교체 확인
```

---

## Uncommitted Changes

### Modified (mc repo)

| 파일 | 변경 내용 |
|------|----------|
| `config/prompts.yaml` | 5개 카테고리에 `cta_phrases` 추가 |
| `chain_card_injector.py` | `get_cta()` 기본값 변경 + `DualCTAInjector` conv CTA 폐기 |
| `chain_publisher_core.py` | D8 CTA leak 패턴 4개 추가 |

### Modified (이전 세션에서 미커밋, 이 세션에서 변경 없음)

| 파일 | 내용 |
|------|------|
| `config/chain_config.yaml` | Phase 15: issue.techpawz permalink_pattern `/posts/:slug/` → `/:slug/` |
| `STATE.md` | Phase 15 hotfix 반영, pytest 246/246 |
| `ROADMAP.md` | Phase 15 hotfix 추가 |
| `.planning/phase-17/PLAN.md` | Phase 17 DoD 업데이트 |

### Untracked (mc repo)

| 파일/디렉토리 | 내용 |
|--------------|------|
| `.planning/phase-16/` | Phase 16 planning artifacts |
| `.planning/phase-17/CTA-SCENARIO.md` | Phase 17 CTA 설계 산출물 |
| `.planning/phase-18/` | Phase 18 planning artifacts |
| `.planning/phase-19/` | Phase 19 planning artifacts |
| `CONTEXT.md` | 열린 잔존 과제 정리 |
| `docs/diagnostics/*.md` | 진단 문서 3건 |

### Modified (mc repo 외부 — Hugo 사이트)

| 파일 | 변경 내용 |
|------|----------|
| `~/Projects/rotcha-blog/layouts/shortcodes/dual-cta.html` | info CTA only, 빨간 버튼 |
| `~/Projects/issue-techpawz-hugo/layouts/shortcodes/dual-cta.html` | 동일 |
| `~/Projects/techpawz-hugo/layouts/shortcodes/dual-cta.html` | 동일 |

---

## W3 완료 요약 (1줄)

**Phase 17 CTA 코드화 완료: cta_phrases 5카테고리 추가, CTA "더 알아보기" 통일, DualCTA conv CTA 폐기, D8 릭 패턴 확장, dual-cta shortcode 3사이트 빨간 버튼 적용. pytest 246/246. 잔존: hub_url NULL 저장 로직 별도 과제.**
