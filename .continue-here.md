# Continue Here — mc

**Created:** 2026-08-02 (Phase 36 최종 완료 후 핸드오프)
**Session focus:** Phase 36 e2e 실증 커밋 + 최종 마무리
**Branch:** `feat/keyword-category-template` (로컬 커밋만 — push 금지 상태)

---

## Current Position

- **Phase 36 완료** (정상 초안 생성 복구 + 안전망 잔여 구멍 차단):
  - 코드 수정 완료(검증됨) + end-to-end 실증 완료(검증됨) → PLAN 완료조건 3 충족.
  - **push/실발행은 대표 최종 승인 시에만** (다음 세션에서 실발행 게이트 진행 예정).
- 커밋 4건: 5000 `1dff13638` / mc `a9b27f5`(수정) + `4c43ba0`(SUMMARY) + `a457591`(e2e) + `de3bb43`(SUMMARY 최종).
- **다음 작업 = 실발행 세션** (아래 Remaining Work 참조).

---

## Completed This Session

### 1. Phase 36 end-to-end 실증 (커밋 `a457591`)
- derive만 dry-run 3회 (DB 쓰기 없음) → 체인 1개(10015)만 DB 저장 → draft → dry-run 발행 게이트.
- 실증 키워드: `스파라쿠아` (etc→depth, 10006/10007 용인로만바스와 동일 조건).
- 결과: derive 3회 모두 BUG-001/002 통과 (truncation 재시도 실발동 실측 — len=281, 8870 두 번), 게이트 비율 0.0/0.0/0.036, dry-run 본문/FM/protected 릭 전부 0건, pytest **907 passed**.
- 커밋 대상: `e2e_derive_dryrun.py`, `e2e_draft_only.py`, `e2e_publish_dryrun.py` + `.planning/phase36/e2e/` 6건 (9파일, 505+).

### 2. Phase 36 최종 완료 기록 (커밋 `de3bb43`)
- `.planning/phase36/PHASE36-SUMMARY.md` 갱신 — 완료(검증됨) / 미검증(실발행 게이트 이관) / 실발행 조건(a·b·c) / 커밋 해시 4건 정리.

### 3. 정리
- `e2e_save_and_draft.py` 삭제 (10016 중복 체인 유발 스크립트 — 커밋 제외).
- DB 스냅샷 최종 확인: chains 10012/10015만 존재, 10016 삭제 반영, publish_log 105건 무변경.

---

## Remaining Work

### 실발행 세션 (대표 최종 승인 후)
1. **(a)** 발행 대상 정상 초안 확보 — 게이트 통과 초안 (chain 10015 계열, `스파라쿠아` step 1/2/3 draft 완성) 또는 신규 생성.
2. **(b)** 실배포 dry-run → 실행 게이트 — hugo build → R2 업로드 → wrangler deploy 단계별 확인.
   - 명령: `env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./public --project-name <사이트>` (배포 규칙: AGENTS.md 섹션 3).
3. **(c)** 대표 최종 승인 후 push/발행.

### 미검증 항목 (실발행 게이트로 이관됨)
- 다른 카테고리(travel/lateral/stock/automotive)에서 BUG-001/002 실효성 미실증.
- draft 비결정성 (draft 1회만 생성, 체인 1개 제한).
- Hugo/R2/deploy 실배포, 이미지/카드 주입/smoke test.

---

## Decisions

- 실증 방식: **derive만 dry-run 3회 → 정상 응답으로 체인 1개만 저장** (사용자 선택, "신규 1개 제한" vs "2~3회 반복" 충돌 해소).
- 실증 키워드: `스파라쿠아` — 10006/10007과 동일 조건(etc→depth) 대조 최적.
- e2e 산출물은 production/test 코드 변경 없이 산출물·스크립트만 커밋.
- `e2e_save_and_draft.py`(create_chain 재호출로 중복 체인 유발)는 커밋 제외·삭제. 이후 `e2e_draft_only.py` 사용.

---

## Blockers

- **push/배포/실발행 금지** — 대표 최종 승인 전까지 (Phase 36 지시 + AGENTS.md).
- DB 쓰기 금지 (실증 종료) — 10015는 실발행 전까지 그대로 유지해야 실증 기록과 일치.
- git checkout/stash/reset 금지.

---

## Bugs & Issues Discovered

### 1. [해결] e2e 재실행 시 DB 중복 체인 생성 (10016)
- **증상**: `e2e_save_and_draft.py`를 백그라운드(nohup)로 재실행하자 `create_chain`이 재호출되어 chain 10016이 중복 생성됨. draft 단계에서 10분 타임아웃 → 프로세스 kill 후 10016 잔존.
- **원인**: 스크립트가 create_chain(신규 체인) + draft를 한 번에 수행하는 구조. 백그라운드 재실행 시 체인 중복 생성. 백그라운드 재실행의 정체는 자동 파이프라인/스케줄러 아님 (CronManager tasks=0, crontab 없음, 잔여 프로세스 없음).
- **해결책**: 10016 삭제 (DELETE chains WHERE id=10016), `e2e_save_and_draft.py` 삭제, `e2e_draft_only.py`(create_chain 없음) 신규 작성.
- **미해결 여부**: 해결됨 — SUMMARY 위반 감지 이력 #3에 기록.

### 2. [해결] 초안 생성 10분 타임아웃
- **증상**: draft_chain이 3 posts × (검색 컨텍스트 + AI 초안)로 194.6초 소요. 전경 실행 시 600초 타임아웃 초과.
- **원인**: 단순 실행 시간 초과 (AI 호출당 20~80초 × 다수 호출).
- **해결책**: `nohup ... &` 백그라운드 실행 + 로그 폴링.
- **미해결 여부**: 해결됨 (다만 실발행 시에도 draft는 백그라운드/충분한 타임아웃 권장).

---

## Environment Notes

- **배포 시 `CLOUDFLARE_API_TOKEN` 충돌**: OpenCode 세션마다 자동 설정됨 → `env -u CLOUDFLARE_API_TOKEN` 필수 (AGENTS.md 섹션 3).
- **Hugo 빌드**: `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify`.
- **DB 위치**: `/Users/twinssn/Projects/5000/data/mc_chains.db` (mc의 `chain_db.py`가 5000 레포 DB 사용).
- **sys.path**: `from shared import ai_writer` 단독 호출 시 ModuleNotFoundError — `mc_paths` import가 5000을 sys.path에 주입하므로 mc_paths/chain_drafter 먼저 import.
- **실증 데이터**: chain 10015 (스파라쿠아, derived), posts 10009~10011 (drafted, slug `스파라쿠아-20260802-s1~3`).
- 5000 레포에 무관 미커밋 파일(config/blogs.d/*.yaml 등) 존재 — 손대지 않음.

---

## Resume Instructions

```bash
# 1. 상태 확인
cd /Users/twinssn/projects2/mc
git log --oneline -3        # 기대: de3bb43, a457591, 4c43ba0
git status --short          # 기대: clean

# 2. 실증 산출물 확인 (선택)
python3 -c "
import json; d=json.load(open('.planning/phase36/e2e/draft_chain10015.json'))
print([(p['step'], p['draft_md_len']) for p in d['posts']])"
python3 -c "
import json; d=json.load(open('.planning/phase36/e2e/publish_dryrun_chain10015.json'))
print(d['all_clean'])"

# 3. 실발행 세션 시작 (대표 최종 승인 후)
#    a. 정상 초안 확보 (10015 계열 또는 신규)
#    b. 실배포 dry-run → 실행 게이트 (hugo build → R2 → deploy)
#    c. push/발행
```

**Resume note**: Phase 36은 완료. 다음 세션은 **실발행 게이트** (위 Remaining Work). push/배포는 대표 최종 승인 후에만.

---

## Uncommitted Changes

- **없음** — `git status --short` 결과 CLEAN (마지막 커밋 `de3bb43`).
- 미푸시 커밋 존재 (로컬만): `de3bb43` 포함 5건 — push 금지 상태이므로 그대로 둠.
