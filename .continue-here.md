# .continue-here.md — mc (Manual Chain)

**Created:** 2026-07-24  
**Session:** 영덕파나크 발행 테스트 + mc CLI 버그 수정 + 문서 업데이트  
**Branch:** `feat/keyword-category-template`

---

## Current Position

- **Active work:** `cli/mc.py`에 `--search`/`--no-search` 옵션 추가 + `chain_card_injector.py` 수정 중
- **Phase context:** Phase 20 완료 (`--search global default`, stock/automotive GROUNDING)
- **Keyword under test:** `영덕파나크` (travel / lateral)
- **Last verified command:** `mc "영덕파나크" --draft` 성공, `--image`도 성공
- **Next intended action:** full publish 실발행 또는 이미지 병목 최적화

---

## Completed This Session

- [x] `mc` CLI에 `--search`/`--no-search` 옵션 추가
- [x] `run_chain(...)`에 `use_context` 전달 반영
- [x] `pip install -e .` 재설치 및 옵션 인식 확인
- [x] `mc "영덕파나크" --dry-run` → Chain #122 생성 확인
- [x] `mc "영덕파나크" --draft` → Chain #123 draft 성공
- [x] `mc "영덕파나크" --image` → Chain #124 image 생성 성공
- [x] STATE.md / AGENTS.md를 코드 기준으로 업데이트
- [x] 주식·여행 키워드 매핑 및 `--search global default` 반영 확인

---

## Remaining Work

1. `영덕파나크` full publish 재시도 또는 단계별 실행
2. 이미지 생성 병목 원인 확인 후 경감
3. Phase 21 `keyword-category-template` 작업 계속
4. 필요 시 `chain_card_injector.py` 수정 내용 커밋

---

## Decisions

- 모든 체인 발행에 네이버 검색 컨텍스트를 **default on**으로 유지
- `mc` CLI에서 `--search` 생략 시에도 검색이 적용되어야 함
- 옵션 충돌(예: `--draft` + `--image`)은 기존 제한 유지

---

## Blockers

- 없음. `mc "영덕파나크" --draft` / `--image` 정상 동작 확인 완료

---

## Bugs & Issues Discovered

### BUG-01: `mc` CLI에서 `--search` 옵션 미노출

- **증상:** `mc "키워드" --search --dry-run` 실행 시 `unrecognized arguments: --search`
- **원인:** `cli/mc.py`에 `--search`/`--no-search` 인자가 정의되지 않았음. `chain_publisher.py`에는 존재하나 mc 래pper에서 노출되지 않음
- **해결책:** `cli/mc.py`에 `--search` 기본 True, `--no-search` 추가하고 `run_chain(use_context=not args.no_search)` 전달하도록 수정
- **미해결 여부:** 다음 세션에서 full publish로 추가 검증 필요

### BUG-02: 이미지 생성 단계에서 발행이 매우 느림

- **증상:** `mc "영덕파나크" --image` 완료까지 약 182초 소요. 사용자가 "너무 오래걸린다"고 확인
- **원인:** 코드 분석 결과, 포스트 1개당 content 이미지와 thumbnail 이미지를 **별도로** 생성/다운로드/R2 업로드하며, 포스트 간 `time.sleep(15s)`이 추가됨
- **해결책:** 미적용. 다음 세션에서 thumbnail 재사용/병렬화/rate-limit 조정 중 선택 필요
- **미해결 여부:** 다음 세션에서 최적화 작업 예정

---

## Environment Notes

- Python 가상환경: `/Users/twinssn/.kaggle-env`
- `mc` 명령은 editable install(`pip install -e .`)로 유지
- Cloudflare 배포 시 `CLOUDFLARE_API_TOKEN` unset 규칙 적용
- R2 버킷 분기: rotcha/informationhot → `hotissue-images`, techpawz → `techpawz-images`

---

## Uncommitted Changes

```
 M AGENTS.md
 M STATE.md
 M chain_card_injector.py
 M cli/mc.py
```

---

## Resume Instructions

```bash
cd /Users/twinssn/projects2/mc

# 1) 변경 상태 확인
git status --short

# 2) 테스트/발행 재개
mc "영덕파나크" --dry-run
mc "영덕파나크" --draft
mc "영덕파나크" --image
mc "영덕파나크" --publish

# 3) 이미지 병목 최적화 필요 시
# - chain_publisher.py generate_chain_images() 부분 검토
# - thumbnail 재사용 또는 업로드 병합 검토

# 4) 필요 시 editable install 재적용
pip install -e .

# 5) Hugo 배포 예시
cd /Users/twinssn/Projects/rotcha-blog && \
HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify && \
env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./public --project-name rotcha-blog
```
