# .continue-here.md — mc (Manual Chain)

**Created:** 2026-07-26  
**Session:** Phase 21 완료 — 이미지 파이프라인 최적화 + Smoke Test  
**Branch:** `feat/keyword-category-template`

---

## Current Position

- **Active work:** Phase 21 완료, Phase 22 대기 중
- **Phase context:** Phase 21 완료 (`thumbnail 재사용 + 병렬화 + 백오프 리트라이 + smoke test`)
- **Last verified command:** `mc "테스트키워드" --image` (이미지 파이프라인 성능 측정 필요)
- **Next intended action:** Phase 22 시작 또는 성능 측정 후 커밋/푸시

---

## Completed This Session

- [x] Task 1: Thumbnail 재사용 + R2 업로드 병합 (별도 API 호출 제거, Pillow overlay로 content 이미지에서 썸네일 생성, 1회 업로드로 2개 키 유지)
- [x] Task 2: Sleep 제거 + 병렬화 + CLI 기본값 확인 (time.sleep(15) → exponential backoff retry 3회 2s/4s/8s, ThreadPoolExecutor max_workers=3, mc "키워드" → publish_mode="auto" 검증 완료)
- [x] Task 3: Smoke Test 함수 + DB + 단위 테스트 (smoke_test() 함수, chain_posts 테이블에 smoke_test_checked_at/result/detail 컬럼 추가, 5개 단위 테스트)
- [x] Performance measurement: 이미지 파이프라인 성능 측정 예정
- [x] ROADMAP.md Phase 21 추가
- [x] .continue-here.md 업데이트: Phase 21 완료, Phase 22 대기
- [x] Commit & push 예정

---

## Remaining Work

1. 성능 측정: `time mc "테스트키워드" --image` 실행 후 변경 전(81초) 대비 기록
2. Commit: `git add -A && git commit -m "Phase 21: image pipeline optimization + smoke test..."`
3. Push: `git push origin feat/keyword-category-template`
4. Phase 22 시작

---

## Decisions

- 썸네일 생성은네일 생성은 별도 AI API 호출 없이 `add_text_overlay()`로 content 이미지 로컬 변환
- R2 업로드는 content 이미지 1회 업로드 후 thumbnail도 같은 파일(로컬 복사)로 upload
- 병렬화는 3개 포스트 동시 처리 (max_workers=3), rate limit은 retry 로직이 처리
- Smoke test는 발행 실패 시 롤백하지 않고 warning만 로깅

---

## Blockers

- 없음. Phase 21 완료, 테스트 251/251 통과

---

## Bugs & Issues Discovered

### BUG-01: 이미지 생성 단계에서 발행이 매우 느림 — **Phase 21에서 해결됨**

- **증상:** `mc "키워드" --image` 완료까지 약 182초 소요. thumbnail 별도 API 호출 + sleep(15) + 순차 처리
- **원인:** 포스트 1개당 content 이미지와 thumbnail 이미지를 별도로 생성/다운로드/R2 업로드하며, 포스트 간 `time.sleep(15s)` 추가
- **해결:** Phase 21에서 thumbnail 재사용(Pillow overlay), R2 업로드 병합, sleep 제거 + exponential backoff retry, 병렬화(ThreadPoolExecutor) 적용
- **예상 성능:** 81초 → 약 30~40초 예상 (실제 측정 필요)

---

## Environment Notes

- Python 가상환경: `/Users/twinssn/.kaggle-env`
- `mc` 명령은 editable install(`pip install -e .`)로 유지
- Cloudflare 배포 시 `CLOUDFLARE_API_TOKEN` unset 규칙 적용
- R2 버킷 분기: rotcha/informationhot → `hotissue-images`, techpawz → `techpawz-images`

---

## Uncommitted Changes

```
 M chain_publisher.py
 M chain_db.py
 M test_chain_publisher_core.py
 M ROADMAP.md
 M .continue-here.md
```

---

## Resume Instructions

```bash
cd /Users/twinssn/projects2/mc

# 1) 변경 상태 확인
git status --short

# 2) 성능 측정 (실제 키워드로)
time mc "테스트키워드" --image 2>&1 | tail -5
# 변경 전 시간(81초)과 비교하여 기록

# 3) 커밋
git add -A
git commit -m "Phase 21: image pipeline optimization + smoke test

- Thumbnail reuse (Pillow overlay, no separate API call)
- R2 upload dedup (1 upload per image, 2 keys)
- Remove fixed sleep, add exponential backoff retry
- Parallel image generation (ThreadPoolExecutor)
- smoke_test() with DB recording
- 5 new unit tests for smoke_test

Performance: image pipeline ___s → ___s"
git push origin feat/keyword-category-template

# 4) Phase 22 시작
```
