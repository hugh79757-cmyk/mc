# Continue Here — mc

**Created:** 2026-07-26 21:30 KST
**Session focus:** Hotfix: techpawz external link card duplication bug

---

## Current Position

Phase 24 (콘텐츠 차별화 + 운영 실증) — Draft mode.
Hotfix (Phase 24 Task 1-2 priority override) **완료**.

---

## Completed This Session

1. **D9 게이트 확장 (chain_card_injector.py)**
   - Raw HTML 외부 링크 카드 제거 로직 추가 (3가지 패턴: primary/secondary/fallback)
   - dual-cta shortcode 제거 로직 추가
   - 정확한 HTML 패턴 매칭 방식 사용 (텍스트 기반 오탐 방지)

2. **단위 테스트 5개 추가 (test_w3_cards_image.py)**
   - TestD9GateExternalCardDedup 클래스:
     - `test_external_link_card_single_in_draft` — 외부 링크 카드 1개 → inject 후 1개
     - `test_external_link_card_duplicate_in_draft` — 2개 중복 → inject 후 1개
     - `test_external_link_card_zero_in_draft` — 0개 → inject 후 1개 추가
     - `test_dual_cta_shortcode_duplicate_removed` — dual-cta 중복 제거
     - `test_no_false_positive_on_body_text` — 일반 문장 오탐 없음 확인

3. **기존 중복 포스트 정리 (2개 체인)**
   - 부산-광안리-20260724-s3 (카드 2회→1개) ✅
   - ai프롬프트마켓-20260722-s3 (카드 2회→1개) ✅
   - Hugo 빌드 + Wrangler Pages 배포 완료
   - `curl`로 라이브 검증 (각 1개 매치)

4. **검증**
   - pytest: 291/291 통과 (회귀 없음)
   - Local grep 확인: 각 포스트 1개 카드
   - Live curl 확인: 각 포스트 1개 카드

5. **커밋 & 푸시**
   - `2a5fc5e` hotfix: D9 gate - remove duplicate external link cards on techpawz
   - Branch: `feat/keyword-category-template`

---

## Remaining Work (Phase 24)

### Task 1: Persona 어조 설계 + 적용
- [ ] `config/personas.yaml` 신규 생성 (site×depth별 role/tone/style/forbidden)
- [ ] `chain_drafter.py` persona 주입 로직
- [ ] 단위 테스트

### Task 2: 카테고리별 검색/품질 개선
- [ ] Travel compare_sources 설정 추가
- [ ] Automotive grounding 설정 (grounding: false)
- [ ] quality_gates.enforce 토글 설정 추가
- [ ] 프롬프트 주입

### Task 3: 운영 실증 — 키워드 10개 자동 발행
- [ ] 키워드 선정
- [ ] `mc auto` 10회 실행
- [ ] RESULTS.md 기록

---

## Decisions

| 의사결정 | 내용 |
|----------|------|
| 외부 링크 카드 중복 제거 방식 | **정확한 HTML 패턴 매칭** 선택 (텍스트 기반 오탐 방지). 3개 패턴(primary/secondary/fallback) 개별 매칭 |
| dual-cta shortcode도 D9 게이트에 포함 | 기존 chain-card/chain-official-card와 동일한 `\{\{<\s*dual-cta\s+.*?\}\}` 패턴으로 제거 |
| 깨진 DB 복구 | 기존 Hugo 파일만 존재하고 DB에는 없는 포스트들을 DB에 등록. hugo_file_path, published_url, draft_md 필드 직접 업데이트 |

---

## Blockers

없음.

---

## Bugs & Issues Discovered

### Bug 1: 외부 링크 카드 중복
- **증상:** techpawz Depth 2 포스트에 외부 링크 카드 HTML이 2회 중복 주입됨
- **원인:** `inject_cards_into_draft()`의 D9 게이트가 Hugo shortcode(`\{\{< chain-card >\}\}`)만 제거하고, `build_external_link_card()`가 생성하는 raw HTML `<div>` 블록은 제거하지 않았음. 이미 주입된 카드가 재주입될 때 제거되지 않고 누적됨
- **해결책:** D9 게이트에 3개 패턴(primary/secondary/fallback) 정규식 추가로 raw HTML 외부 링크 카드도 제거
- **미해결 여부:** 해결됨 (닫힘)

### Bug 2: dual-cta shortcode 중복 가능
- **증상:** D9 게이트가 dual-cta shortcode를 제거하지 않아 중복 주입 가능성
- **원인:** D9 게이트가 chain-card/chain-official-card만 커버
- **해결책:** dual-cta shortcode도 D9 게이트 제거 대상에 추가
- **미해결 여부:** 해결됨 (닫힘)

---

## Environment Notes

- **Branch:** `feat/keyword-category-template`
- **Settings:** `/Users/twinssn/.config/opencode/AGENTS.md` — AdSense 매핑, Hugo/Wrangler 배포 규칙 적용됨
- **DB Path:** `/Users/twinssn/Projects/5000/data/mc_chains.db`
- **Hugo Sites:** `/Users/twinssn/Projects/{rotcha-blog, issue-techpawz-hugo, techpawz-hugo}`
- **Shared Themes:** `/Users/twinssn/Projects/shared-themes`
- **Cloudflare:** Wrangler Pages (techpawz-hugo project) — `env -u CLOUDFLARE_API_TOKEN` 사용 필요
- **Python:** Python 3.14.5, pytest 9.1.1

---

## Resume Instructions

```bash
# 현재 브랜치 확인
git branch | grep feat/keyword-category-template

# pytest 기준선 확인
cd /Users/twinssn/projects2/mc && python -m pytest

# Phase 24 Task 1 시작: persona 설계
# 1. config/personas.yaml 생성
# 2. chain_drafter.py 수정 (persona 주입)
# 3. 단위 테스트 추가
# 4. pytest 검증

# Phase 24 Task 2: 카테고리별 검색/품질 개선
# 1. config/prompts.yaml 수정 (travel.compare_sources)
# 2. chain_drafter.py 수정 (compare_sources 주입)
# 3. config/chain_config.yaml (quality_gates.enforce)
# 4. pytest 검증

# Phase 24 Task 3 (가장 긴 작업, 마지막 실행):
# mc auto --keyword "..."  # 10개 키워드 순차 실행
```

---

## Uncommitted Changes

- **`.planning/phase-24/`** — Phase 24 planning artifacts (PLAN.md, CONTEXT.md, RESEARCH.md, VERIFICATION.md) — 미추적 상태, Phase 24 완료 시점에 commit 예정
