# .continue-here.md — mc (Manual Chain) — W3 카드 재설계 완료

**Updated:** 2026-07-23
**Phase:** Phase 14 + P1 + P2 + R2 + W3 카드 재설계
**Status:** ✅ 215/215, 라이브 3/3 카드, BS4 제거, 화이트리스트 7/10

---

## 이번 세션 완료 요약

### W3 — 카드 재설계 (4 서브태스트 + 이미지 프롬프트)

| 서브태스크 | 내용 | 테스트 |
|-----------|------|--------|
| W3-A | 카드 3단계 (Depth0/1: 다음글, Depth2: 외부링크) | 4개 |
| W3-B | 외부링크 추출 (Naver API + fallback, BS4 제거) | 10개 |
| W3-C | 내용 겸침 방지 (프롬프트 역할 분담) | 3개 |
| W3-이미지 | Pollinations 프롬프트 규칙 (인물금지/스타일/주제시각화) | 10개 |
| 합계 | +36 신규 테스트 | 215/215 |

### W3 주요 변경 파일

- `chain_card_injector.py`: `_classify_authority()` 서브도메인 수정 + `_search_via_api()` + `build_external_link_card()` + 화이트리스트 3건 제거
- `chain_publisher.py`: `inject_cards_chain()` range 확장 + Depth 2 외부링크 + `_deploy_blogs_after_inject()`
- `image/prompt_builder.py`: `_infer_topic_type()` + `_select_style()` + `NO_PEOPLE_BLOCK` + `build_full_prompt()` 중복 제거
- `config/prompts.yaml`: depth별 역할 분담 + 중복 금지
- `scripts/verify_whitelist.py`: 화이트리스트 HTTP 검증 스크립트 신규

### 라이브 검증

| 체인 | rotcha | infohot | techpawz |
|------|--------|---------|----------|
| #74 파주 옳은휴식하루 | ✅ | ✅ | ✅ (Naver search) |
| #50 욕지도배편 | ✅ | ✅ | ✅ (공공기관) |
| #28 포천계곡펜션 | ❌ | ✅ | ✅ (공공기관) |

---

## Current Baseline

| 항목 | 값 |
|------|-----|
| pytest | **215/215** ✅ |
| 라이브 | 3/3 카드 주입 + 배포 (#74, #50 완전, #28 2/3) |
| BS4 스크래핑 | 제거 완료 (API only) |
| 화이트리스트 | 7/10 (ferrypark/airbusan/phr 제거) |
| `mc` 전역 명령 | ✅ `/Users/twinssn/.kaggle-env/bin/mc` |

---

## 이월 (별도 milestone)

1. **#28 rotcha Hugo 빌드 실패** — ````json` 미정리, 콘텐츠 수정 필요 (W3과 무관)
2. **Phase 14.1** — cron/launchd 스케줄링, dashboard CLI, `audit_chain.py` 통합
3. **(a) 43건 고아** — 신규 발행 W6 게이트로 차단
4. **P3 Blowfish CSS** — 라이브 3/3 기능 정상, CSS 미세 복구 영역

---

## Bugs & Issues Discovered

### 1. #28 rotcha Hugo 빌드 실패 (W3에서 발견, W3과 무관)
- **증상**: `unknown shortcode token "HAHAHUGOSHORTCODE..."` 
- **근인**: 기존 `draft_md` 끝에 ````json` 코드 펜스 미정리 → 카드 shortcode가 펜스 안에 들어감
- **확인**: chain #50, #74에는 ````json` 없음 → #28만 해당
- **수정 방향**: ````json` 코드 펜스 제거 후 재발행
- **우선순위**: 낮음 (기존 콘텐츠 문제, 신규 발행에 영향 없음)

### 2. 화이트리스트 도메인 3건 제거
- ferrypark.co.kr, airbusan.com, phr.co.kr → HTTP 연결 실패
- `scripts/verify_whitelist.py`로 향후 검증 가능

---

## Environment

| 항목 | 값 |
|------|-----|
| OS | macOS |
| Python | 3.14 |
| Runtime | opencode |
| Working dir | `/Users/twinssn/projects2/mc` |
| Hugo sites | `/Users/twinssn/Projects/{rotcha-blog, informationhot-hugo, techpawz-hugo}` |
| Chain DB | `/Users/twinssn/Projects/5000/data/mc_chains.db` |
| Shared themes | `/Users/twinssn/Projects/shared-themes` |

---

## Resume Instructions

```bash
cd /Users/twinssn/projects2/mc

# 상태 확인
git status --short
python -m pytest --tb=short -q
python scripts/verify_whitelist.py

# Phase 14 CLI
mc "새키워드"                  # full pipeline
mc "새키워드" --dry-run        # derive only
mc --chain-id N --resume      # 재개

# 카드 주입 (기존 체인)
python -c "from chain_publisher import inject_cards_chain; inject_cards_chain(CHAIN_ID)"

# Hugo 배포 (rotcha 예시)
cd /Users/twinssn/Projects/rotcha-blog && HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify && env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./public --project-name rotcha-blog
```

---

## W3 완료 요약 (1줄)

**W3 카드 재설계 완료: 3단계 카드 체계 + Naver API 외부링크 + 내용 역할 분담 + Pollinations 이미지 규칙. pytest 215/215. 라이브 3/3 카드 주입. BS4 스크래핑 제거. 화이트리스트 7/10. 잔존: #28 rotcha json fence 미정리 (기존 콘텐츠, W3 무관).**
