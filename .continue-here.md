# .continue-here.md — mc (Manual Chain) — Phase 13 완료

**Updated:** 2026-07-22
**Phase:** Phase 13 — 콘텐츠 고도화 (Wave 1 + Wave 2) ✅
**Status:** 머지 완료, pytest 130/130, 업클로젯 체인 #66 라이브 3/3

---

## 마지막 세션 요약

Phase 13에서 수행한 작업:

### Wave 1 — Markdown Cleanup + Table Fix (R2+R3)
1. **`_clean_markdown_symbols()` 이관** — Hugo 포스트의 raw LLM 출력 정제 함수
   - `|`, `##`, `**`, ````, `{{`/`}}` 등 leak 방지
   - 테이블(`| ... |`)과 코드블록/인라인코드(math 포함) 보호
2. **파이프 규칙 강화** (`config/prompts.yaml`) — 불필요한 특수문자 사용 금지
3. **CJK Bold Spacing 버그 발견 및 수정**
   - 원인: `_clean_markdown_symbols()`의 Rule 2가 `**중요한**` → `** 중요 한 **` 변환
   - 긴급 조치: Rule 2(전체 CJK 간격) 제거, AI 프롬프트로 `볼드체 앞뒤 공백` 보장
   - 재발행 후 CDN 캐시 버스팅으로 3/3 사이트 `<strong>` 정상 렌더링 확인
4. **단위 테스트 10개** (TestCleanMarkdownSymbols — test_chain_publisher_core.py)

### Wave 2 — Contextual Image Enhancement (R1)
1. **`build_contextual_prompt()`** (`image/prompt_builder.py`) — title+angle 기반 Pollinates 프롬프트
2. **`search_providers.py`** (신규) — Unsplash + Pexels 검색 + 24h LRU 캐시 + 상호 fallback
3. **`_publish_hugo()` photo 분기** — body-image photo: search → contextual Pollinates fallback
4. **단위 테스트 8개** (2 in test_image_pipeline.py + 6 in test_image_search.py)

### 라이브 검증
- **체인 #66 (업클로젯)** → rotcha/informationhot/techpawz 3/3 ✅
- 검증 항목: HTTP 200, H2≥1, 이미지 주입, 표 렌더링, `<strong>` 렌더링, `**`/`|`/`##` leak 0

## Current Baseline

- **pytest:** **130/130** ✅ (112 기존 + 18 신규)
- **라이브:** 업클로젯 체인 #66 — rotcha/infohot/techpawz 3/3 ✅
- **머지:** Phase 13 전체 main에 머지 완료

---

## 인계 사항

### (a) Phase 14 — CLI 단일 진입점 `mc <keyword>`
- **상태:** PLAN.md + CONTEXT.md 기획 완료 (구현 전)
- **범위:** `mc <키워드>` → `chain_publisher.py --draft / --image / --publish` 순차 호출
- **옵션:** `--dry-run`, `--site`, `--skip-publish`, `--help`, `--resume`, `--background`
- **제약:** `chain_publisher.py` 수정 금지, 기존 19/27/Phase 11~13 건드리지 않음
- **pytest 목표:** 130/130 유지 + N개 신규
- **작업:** 구현만 하면 됨 (기획 완료)

### (b) Phase 14.1 — cron/launchd + dashboard + audit 통합
- **상태:** Phase 14 구현 후로 이월
- **범위:** cron 스케줄링, dashboard CLI, `audit_chain.py` 통합

### (c) 고아 NULL content_image_path 정리 (이월 — Phase 11에서 계속)
- **상태:** image_meta JSON은 존재 but content_image_path가 NULL인 포스트 37건
- **우선순위:** P2 (중간)
- **명령:** `python -c "from chain_db import get_conn; ..."`

### (d) Blowfish CSS 복구 (이월)
- **상태:** Blowfish 테마 CSS 일부 누락 (레이아웃 경미한 깨짐)
- **우선순위:** P3 (낮음)

---

## Environment

| 항목 | 값 |
|------|-----|
| OS | macOS |
| Python | 3.14 |
| Runtime | opencode |
| Working dir | `/Users/twinssn/projects2/mc` |
| Hugo sites | `/Users/twinssn/Projects/{rotcha-blog, informationhot-hugo, techpawz-hugo}` |
| Chain DB | `/Users/twinssn/Projects/5000/data/mc_chains.db` |
| Shared themes | `/Users/twinssn/Projects/shared-themes` |

---

## Resume Commands

```bash
cd /Users/twinssn/projects2/mc

# 상태 확인
git status --short
python -m pytest --tb=short -q

# Phase 14 구현 (기획 완료, 구현 필요)
# PLAN.md: .planning/phase-14/PLAN.md

# 새 체인 발행 (기존 방식)
python chain_publisher.py --seed "키워드"
python chain_publisher.py --chain-id <id> --draft
python chain_publisher.py --chain-id <id> --image
python chain_publisher.py --chain-id <id> --publish

# 인계 (c) 고아 content_image_path 확인
python -c "
import chain_db as db
conn = db.get_conn()
rows = conn.execute(\"SELECT id, image_meta FROM chain_posts WHERE json_extract(image_meta, '$.content_image_path') IS NULL AND image_meta IS NOT NULL\").fetchall()
print(f'{len(rows)} orphan posts')
"

# Hugo 배포 (rotcha 예시)
cd /Users/twinssn/Projects/rotcha-blog && HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify && env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./public --project-name rotcha-blog
```

---

## Phase 13 요약 (1줄)

**13/13 머지 완료, pytest 130/130, 업클로젯 3/3 라이브 ✅, Phase 14 CLI 기획 완료 (구현 대기)**
