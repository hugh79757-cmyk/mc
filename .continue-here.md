# .continue-here.md — mc (Manual Chain) — 이미지 파이프라인 전체 종료

**Updated:** 2026-07-23
**Phase:** Phase 14 + P1 + P2 + R2 이미지 수정 + techpawz 버킷 분기 완료
**Status:** ✅ 179/179, 라이브 3/3 R2 200, 이미지 파이프라인 종료

---

## 마지막 세션 요약

### Phase 14 완료: CLI 단일 진입점 `mc <keyword>`

4개 Wave, 35개 신규 테스트, 전 Waves 완료:

#### Wave 1 — CLI Wrapper + _run_full() (R1, R2, R4)
- **`cli/mc.py`** (374줄): `main()` argparse + `_run_full()` 파이프라인 위임
- `--dry-run` / `--draft` / `--image` / `--skip-publish` / `--publish` / `--resume`
- `--site rotcha/infohot/techpawz/aikorea24` single-site override
- `_build_blog_overrides()`: site → blog_overrides dict
- 25개 테스트 (argparse 10 + blog_overrides 7 + routing 8)
- Live: `mc 업클로젯 --dry-run` → Chain #67 (27.2s) ✅

#### Wave 2 — _resume_chain() (R3)
- DB 상태 감지: draft_md / image_url / published_url 없는 post 기준
- 완료 단계 자동 스킵, 미완료 단계만 재개
- 잘못된 chain-id → exit 2, 이미 완료 → "already complete" 로그 + exit 0
- 4개 테스트: all-complete / invalid-id / sequential / partial-publish

#### Wave 3 — _setup_logging() + _run_background() (R5, R6)
- `_setup_logging()`: dual handler (file DEBUG + stdout INFO), flush-on-emit
- `_run_background()`: `subprocess.Popen` + `start_new_session=True` + `--pid-file` + atexit cleanup
- 6개 테스트: dual_handler / flush / popen_start_new_session / pid_file_created / argv_includes_pid_file / console_single_line

#### Wave 4 — pip install -e . + console_scripts
- **`pyproject.toml`** (신규): `[project.scripts] mc = "cli.mc:main"` + dependencies + build-system
- `pip install -e .` → `which mc` → `/Users/twinssn/.kaggle-env/bin/mc` ✅
- `mc --help`: R1~R6 전체 인터페이스 출력 ✅
- **Frontmatter injection patch** (`cli/mc.py`): `patch("chain_drafter.draft_chain")` → draft_md에 frontmatter 주입 → `_validate_draft_schema` 통과 (chain_publisher.py 미수정)
- **End-to-end live**: `mc AI프롬프트마켓` → Chain #71 (352.6s) → 3/3 HTTP 200 H2 img ✅

### 라이브 검증
- **체인 #71 (AI프롬프트마켓)** → rotcha/informationhot/techpawz 3/3 ✅
- 검증: HTTP 200, H2≥1, 이미지 존재, logs/mc-cli-20260722.log (35,708 bytes)

---

## Current Baseline

| 항목 | 값 |
|------|-----|
| pytest | **179/179** ✅ (130 기존 + 35 W1-W3 + 6 P1 + 3 published_md + 5 R2 bucket) |
| 라이브 | 3/3 R2 200 ✅ (rotcha/infohot/techpawz 전부) |
| 머지 | Phase 14~R2 전체 main에 머지 완료 |
| 이미지 파이프라인 | ✅ 종료 (W6/P2/published_md/버킷분기 전부 완료) |
| `mc` 전역 명령 | ✅ `/Users/twinssn/.kaggle-env/bin/mc` |

---

## 인계 사항

### ✅ 완료된 작업

| 작업 | 상태 |
|------|------|
| Phase 14 CLI + P1 frontmatter | ✅ 완료 (pytest 179/179) |
| P2 고아 content_image_path | ✅ 완료 (15/15) |
| R2 published_md (card injection URL 보존) | ✅ 완료 |
| R2 techpawz 버킷 분기 | ✅ 완료 (hotissue-images → techpawz-images) |

### 이월 (별도 milestone)

1. **Phase 14.1** — cron/launchd 스케줄링, dashboard CLI, `audit_chain.py` 통합 (공수 큼)
2. **(a) 43건 고아** — 신규 발행 W6 게이트로 차단, 기존 43건은 재발행 전까지 이미지 없음
3. **P3 Blowfish CSS** — 라이브 3/3 기능 정상, CSS 미세 복구 영역

### techpawz-images 마이그레이션 버킷 존중 원칙

- `techpawz-images` 버킷은 WordPress→Hugo 마이그레이션 이미지 보유
- 기존 마이그레이션 이미지 키 구조: `images/techpawz/{timestamp-slug}/{hash}.webp`
- 신규 업로드 키 구조: `images/techpawz/{slug}/{filename}.webp`
- 충돌 없음 (키 구조 다름). 기존 이미지 건드리지 않음.

---

## Environment

| 항목 | 값 |
|------|-----|
| OS | macOS |
| Python | 3.14 |
| Runtime | opencode |
| Working dir | `/Users/twinssn/projects2/mc` |
| Hugo sites | `/Users/twinssn/Projects/{rotcha-blog, informationhot-hugo, techpawz-hugo}` |
| Chain DB | `/Users/twinssn/Projects/5000/data/mc_chains.db` |
| Shared themes | `/Users/twinssn/Projects/shared-themes` |

---

## Resume Commands

```bash
cd /Users/twinssn/projects2/mc

# 상태 확인
git status --short
python -m pytest --tb=short -q

# Phase 14 CLI (완료 — 전역 mc 명령 사용)
mc "새키워드"                  # full pipeline (derive→draft→image→publish)
mc "새키워드" --dry-run        # derive only
mc "새키워드" --draft          # derive + draft
mc "새키워드" --image          # derive + draft + image (skip publish)
mc --chain-id 67 --resume      # 재개
mc "키워드" --site rotcha      # single-site override
mc "키워드" --background       # 백그라운드 실행

# 인계 (c) 고아 content_image_path 확인
python -c "
import chain_db as db
conn = db.get_conn()
rows = conn.execute(\"SELECT id, image_meta FROM chain_posts WHERE json_extract(image_meta, '\$.content_image_path') IS NULL AND image_meta IS NOT NULL\").fetchall()
print(f'{len(rows)} orphan posts')
"

# Hugo 배포 (rotcha 예시)
cd /Users/twinssn/Projects/rotcha-blog && HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify && env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./public --project-name rotcha-blog
```

---

## 이미지 파이프라인 종료 요약 (1줄)

**Phase 14 + P1 + P2 + R2 이미지 수정 + techpawz 버킷 분기 완료. pytest 179/179. 라이브 3/3 R2 200. 이미지 파이프라인 전체 종료. 잔존 이월 3건 (Phase 14.1 / (a)43건 / P3 Blowfish).**