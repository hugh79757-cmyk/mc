# .continue-here.md — mc (Manual Chain) — Phase 11 W6 인계

**Updated:** 2026-07-22
**Phase:** Phase 11 W1~W6 전체 완료 ✅
**Status:** 머지 완료, pytest 112/112, 리린샵 체인 #64 라이브 3/3

---

## 마지막 세션 요약

W6에서 수행한 작업:

1. **스키마 검증 게이트** (`_validate_draft_schema`) — draft 마크다운 발행 전 검증
   - H2 헤딩 ≥1, 이미지 마커 존재, frontmatter 필수 필드, image_keyword 존재 확인
   - chart 마커는 image_keyword 검증 예외
2. **image_keyword 자동 보강** — draft 단계에서 image_keyword 누락 시 제목으로 fallback
   - photo/chart 모두 대상. chart는 썸네일(OG 태그) 생성용.
3. **프롬프트 릭 필터 정규식 수정** — 이스케이프 오류 수정, `**[...]**` 패턴 추가
4. **"리린샵" 체인 #64 발행 성공** — 3/3 라이브 검증 완료
5. **chart fallback 충돌 검증** — image_keyword가 chart 콘텐츠 이미지 생성에 영향 없음 확인

## Current Baseline

- **pytest:** **112/112** ✅
- **라이브:** 리린샵 체인 #64 — rotcha/infohot/techpawz 3/3 ✅
- **머지:** W1~W6 전체 main에 머지 완료

---

## 인계 사항

### (a) W3 잔여 — card_injector shortcode 적용
- **상태:** 기존 1,086건 HTML card_injector 제거 완료
- **잔여:** 신규 발행 건에만 shortcode 적용 중
- **확인:** `chain_card_injector.py`에서 `<!--card-->` → `{{< card >}}` shortcode 변환
- **작업:** 새 체인 발행 시 카드가 Hugo shortcode로 정상 주입되는지 확인

### (b) 고아 NULL content_image_path 정리
- **상태:** image_meta JSON은 존재 (chart_data 등 포함) but content_image_path가 NULL인 포스트 37건
- **원인:** 이전 버전에서 이미지 생성 전에 DB 저장된 케이스
- **작업:** 백필 스크립트 실행 필요 (`find NULL content_image_path → render_chart/thumbnail`)
- **명령:** `python -c "from chain_db import get_conn; ..."`
- **우선순위:** P2 (중간)

### (c) slug 고유화 / 비의도 체인 자동 감지
- **상태:** slug 충돌 가능성 존재. 비의도 체인(의도치 않은 중복 체인 생성) 방지 로직 부재
- **작업:** `_NON_INTENDED_CHAINS` 가드 확장 또는 slug uniqueness enforcement
- **우선순위:** P2 (중간)

### (d) Blowfish CSS 복구
- **상태:** Blowfish 테마 CSS 일부 누락 (레이아웃 경미한 깨짐)
- **작업:** Hugo 테마 업데이트 또는 CSS 오버라이드 복구
- **우선순위:** P3 (낮음)

---

## Environment

| 항목 | 값 |
|------|-----|
| OS | macOS |
| Python | 3.14 |
| Runtime | opencode |
| Working dir | `/Users/twinssn/projects2/mc` |
| Hugo sites | `/Users/twinssn/Projects/{rotcha-blog, informationhot-hugo, techpawz-hugo}` |
| Chain DB | `/Users/twinssn/Projects/5000/data/mc_chains.db` |
| Shared themes | `/Users/twinssn/Projects/shared-themes` |

---

## Resume Commands

```bash
cd /Users/twinssn/projects2/mc

# 상태 확인
git status --short
python -m pytest --tb=short -q

# 새 체인 발행
python chain_publisher.py --seed "키워드"
python chain_publisher.py --chain-id <id> --draft
python chain_publisher.py --chain-id <id> --image
python chain_publisher.py --chain-id <id> --publish

# 인계 (b) 고아 content_image_path 확인
python -c "
import chain_db as db
conn = db.get_conn()
rows = conn.execute(\"SELECT id, image_meta FROM chain_posts WHERE json_extract(image_meta, '$.content_image_path') IS NULL AND image_meta IS NOT NULL\").fetchall()
print(f'{len(rows)} orphan posts')
"

# Hugo 배포 (rotcha 예시)
cd /Users/twinssn/Projects/rotcha-blog && HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify && env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./public --project-name rotcha-blog
```

---

## Phase 11 W6 요약 (1줄)

**W1~W6 머지 완료, pytest 112/112, 리린샵 3/3 라이브✅, 인계: (a) card shortcode (b) 37 orphan (c) slug dedup (d) CSS 복구**
