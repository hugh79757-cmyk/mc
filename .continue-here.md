# Continue Here — mc

**Created:** 2026-08-02 (M2 마일스톤 등록 완료 후 핸드오프)
**Session focus:** 정보가 3-블로그 체인 복제 타당성 조사 (읽기 전용) → M2 마일스톤 등록
**Branch:** `feat/keyword-category-template` (로컬 커밋만 — push 금지 상태)

---

## Current Position

- **Phase 36 완료** (이전 세션): e2e 실증 종료, push/실발행은 대표 최종 승인 대기.
- **이번 세션 — M2 마일스톤 등록 완료 (커밋 `f63ecb1`)**: 정보가 3-블로그(informationhot.kr / kuta.informationhot.kr / 5.informationhot.kr) 체인 복제 작업을 Phase 37~40으로 등록.
  - 읽기 전용 조사 완료: 필수 분리 지점(DB/shared/R2/git) + 정보가 인프라 실측 + chain_config 교체 범위 확정.
  - **다음 작업 = Phase 37 착수** (`/gsd:plan-phase 37`).

---

## Completed This Session

### 1. mc 복제 타당성 조사 (읽기 전용, 수정 없음)
- **필수 분리 지점 확정**:
  - DB: `config/chain_config.yaml:204` `db_path: /Users/twinssn/Projects/5000/data/mc_chains.db` — 5000 레포와 공유 중 → 복제 레포 내 경로로 분리 필수 (미분리 시 크로스 발행).
  - shared: `mc_paths.py:27` `PATH_5000` 하드코딩 + sys.path 주입 → ai_writer.py는 shared.env_loader + 5000/config/models.yaml 의존.
  - R2: `image/r2_uploader.py:29-38` HUGO_R2_DOMAINS — kuta 키만 존재, informationhot/5.informationhot 키 부재 → `chain_publisher_core.py:512/656`에서 이미지 업로드 스킵됨.
  - git: origin 단일(hugh79757-cmyk/mc.git), 현재 브랜치 `feat/keyword-category-template`.
- **정보가 3사이트 인프라 실측**: CF Pages 프로젝트 3개 존재 + 커스텀 도메인 + HTTPS 3/3 200 (`Information HOT`/`KUTALOG`/`TEAM65`). AdSense ca-pub-6677996696534146 3사이트 일치 ✅.
- **미비 5건 (셋업 필요)**: ① shortcode 3종(chain-card/chain-official-card/dual-cta) 3사이트 전부 부재 ② 5.informationhot-hugo baseURL `https://example.org/` 플레이스홀더 ③ ads.txt (kuta/5) 부재 ④ R2 매핑 키 (info/5) 부재 ⑤ 65_informationhot blogger 정의(blog_id 자리표시자 + creds 파일 없음 → 실사용 불가).

### 2. M2 마일스톤 등록 (커밋 `f63ecb1`, 5 files, +218/-15)
- `.planning/ROADMAP.md` — M2 섹션 + Phase 37~40 작업표.
- `.planning/REQUIREMENTS.md` — M2 13건 (CLONE-01~05, INFRA-01~04, CFG-M2-01~05, VERIFY-M2-01~03) + M2 Out of Scope 4건.
- `.planning/PROJECT.md` — Context에 M2 분리 지점 + Key Decisions 3건.
- `.planning/STATE.md` — Current focus → M2, 실측 요약, Next Action에 Phase 37 추가.
- `.planning/phase-37/RESEARCH.md` — 조사 전체 결과 아카이브 (신규).

### 3. 대표 확정 결정 3건 (질문 게이트)
- **마일스톤 범위**: 셋업+검증까지 (복제 레포 + 필수 분리 + 정보가 3사이트 Hugo 셋업 + chain_config 교체 + dry-run 발행 검증). 실발행은 검증 후 별도 승인 게이트.
- **shared 의존성**: vendoring 복사 (ai_writer.py + env_loader.py + __init__.py + config/models.yaml, mc_paths.py PATH_5000 제거).
- **카드 색상**: 체인 카드만 파랑 (#2563eb shortcode 신규, html_renderer.py 구조 유지).

---

## Remaining Work

### Phase 37: 복제 레포 생성 + 필수 분리 (다음 착수)
1. git clone + 새 원격 교체 (기존 mc origin과 분리).
2. `.env` 재생성 (git 미추적 — R2/KREA 키 수동 복사).
3. **DB 분리** — chain_config.yaml `db_path` → 복제 레포 내 경로 (5000 공유 해제).
4. **shared vendoring** — ai_writer.py + env_loader.py + __init__.py + config/models.yaml 복사, PATH_5000 제거.
5. R2 매핑 키 추가 (informationhot/5.informationhot) — prefix/도메인 네이밍 결정 (미확정).

### Phase 38: 정보가 3사이트 Hugo 셋업
- shortcode 3종(파랑 #2563eb) 신규 생성 / 5.informationhot-hugo baseURL 수정 / ads.txt(kuta·5) 추가 / CF Pages 매핑 확인.

### Phase 39: chain_config 교체
- sites/chain_blogs/chain_blog_mapping 정보가 3개로 교체 + 기존 사이트 정의 처리 결정 + 카드 색상 파랑 검증.

### Phase 40: dry-run 발행 검증
- e2e dry-run 3종(e2e_derive_dryrun/e2e_draft_only/e2e_publish_dryrun) + pytest green + DoD 체크리스트.

### Phase 36 이월 — 실발행 세션 (대표 최종 승인 후)
- (a) 발행 대상 정상 초안 확보 → (b) 실배포 dry-run → 실행 게이트 (hugo build → R2 → wrangler deploy) → (c) push/발행.

---

## Decisions

- **M2 마일스톤 등록 방식**: 기존 mc GSD 프로젝트에 새 마일스톤 추가 (사용자 선택 — 별도 프로젝트 아님).
- **마일스톤 범위**: 셋업+검증까지 — 실발행은 검증 후 별도 승인 게이트.
- **shared**: vendoring 복사 (PATH_5000 제거) — 독립 레포 취지.
- **카드 색상**: 체인 카드만 파랑 — html_renderer.py external 카드(빨강 primary)는 구조 유지.
- **push 생략**: 이전 핸드오프의 "push 금지 — 대표 최종 승인 전" 제약 존중. `f63ecb1`은 로컬 유지 (사용자 선택).

---

## Blockers

- **push/배포/실발행 금지** — 대표 최종 승인 전까지 (Phase 36 지시 + AGENTS.md + 이번 세션 확인).
- **DB 쓰기 금지** (조사/등록 세션 종료) — 5000/data/mc_chains.db는 실발행 전까지 무변경.
- git checkout/stash/reset 금지.
- **미확정 5건** (M2 이월, RESEARCH.md §5): R2 prefix/도메인 네이밍 / informationhot-hugo baseURL 내용 / 5000 발행 충돌 정합 / 기존 콘텐츠 slug 정합 / 65_informationhot 처리.

---

## Bugs & Issues Discovered

### 1. [미해결 → 셋업 필수] 정보가 3사이트 shortcode 3종 전부 부재
- **증상**: chain-card/chain-official-card/dual-cta가 informationhot-hugo(linkcard.html만)·kuta-hugo(related-card.html만)·5.informationhot-hugo(shortcodes 디렉토리 없음)에 없음. 카드 주입 시 `{{< chain-card >}}`가 raw text로 노출 또는 Hugo build 실패 위험.
- **원인**: 정보가 3사이트는 기존 rotcha/techpawz 체인 파이프라인 대상이 아니었음 (rotcha/techpawz/issue-techpawz에는 3종 존재 확인).
- **해결책**: Phase 38에서 파랑(#2563eb) shortcode 3종 신규 생성 (체인 카드만 파랑 — 대표 확정).
- **미해결 여부**: 미해결 — Phase 38로 이월.

### 2. [미해결 → 셋업 필수] 5.informationhot-hugo baseURL 플레이스홀더
- **증상**: `hugo.toml`에 `baseURL = 'https://example.org/'`, `title = 'My New Hugo Project'` — 실제 도메인과 불일치.
- **원인**: WordPress(5.informationhot.kr)에서 마이그레이션 중 상태로 추정.
- **해결책**: Phase 38에서 `https://5.informationhot.kr` + 적절한 title/theme 설정으로 교체.
- **미해결 여부**: 미해결 — Phase 38로 이월.

### 3. [미해결 → 셋업 필수] R2 매핑 키 부재 (informationhot / 5.informationhot)
- **증상**: `image/r2_uploader.py` HUGO_R2_DOMAINS에 kuta 키만 존재. informationhot-hugo/5.informationhot-hugo는 get_r2_config → (None, None) → `chain_publisher_core.py:656`에서 이미지 업로드 스킵 → broken image.
- **원인**: 정보가 메인/5번 사이트가 mc 파이프라인 대상이 아니었음.
- **해결책**: Phase 37에서 HUGO_R2_DOMAINS 엔트리 추가 + 실제 R2 커스텀 도메인 바인딩 확인 (img-5.informationhot.kr 등 — 미확정).
- **미해결 여부**: 미해결 — Phase 37/38로 이월.

### 4. [미해결] 65_informationhot blogger 정의 — 실사용 불가 상태
- **증상**: `config/chain_config.yaml:54-63`에 65_informationhot blogger 정의 있으나 `blog_id: BLOGGER_BLOG_ID_HERE` 자리표시자 + `config/blogger_credentials.json` 파일 부재 (실측: mc/config에 blogger 파일 0개).
- **원인**: Phase 15에서 informationhot → issue.techpawz 슬롯 교체 후 방치된 정의.
- **해결책**: 복제 레포에서 제거 또는 보류 — 키 이름이 달라 M2와 충돌 없음. 처리 결정은 Phase 39 CFG-M2-04.
- **미해결 여부**: 미해결 — Phase 39로 이월.

### 5. [미해결] 5000 레포가 정보가 사이트를 관리 중 — 발행 충돌 가능성
- **증상**: 5000 레포 `data/dashboard/routes/api.py:79`에 blog_id "informationhot-hugo" 등록 + scripts(batch_thumbnails/resume_backfill/fix_remaining_posts)가 정보가 사이트 참조.
- **원인**: 정보가 계열은 5000 레포(CUAP) 파이프라인 영역으로 추정.
- **해결책**: mc 복제 레포 발행 시작 전 5000 발행 파이프라인 여부 확인 필요 (미확정 이월).
- **미해결 여부**: 미해결 — M2 이월.

### 6. [해결] 이전 세션 기록 유지
- Phase 36의 BUG-001/002(truncation 재시도), e2e 중복 체인(10016), draft 10분 타임아웃 — 모두 이전 핸드오프 기록 참조 (이번 세션 무관).

---

## Environment Notes

- **배포 시 `CLOUDFLARE_API_TOKEN` 충돌**: OpenCode 세션마다 자동 설정됨 → `env -u CLOUDFLARE_API_TOKEN` 필수 (AGENTS.md 섹션 3). OAuth 프로필: hugh79757 (Account: fac9808c757df31d797190c529aaa71a).
- **Hugo 빌드**: `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify`.
- **DB 위치**: `/Users/twinssn/Projects/5000/data/mc_chains.db` (실존 3.5MB, 8/2 13:34 수정) — M2에서 복제 레포 DB로 분리 예정.
- **sys.path**: `from shared import ai_writer` 단독 호출 시 ModuleNotFoundError — mc_paths import가 5000을 sys.path에 주입.
- **정보가 3사이트 실측**: CF Pages 프로젝트명 = informationhot-hugo / kuta-hugo / 5-informationhot. HTTPS 3/3 200. AdSense 3사이트 모두 ca-pub-6677996696534146.
- **Hugo 디렉토리**: `/Users/twinssn/Projects/{informationhot-hugo, kuta-hugo, 5.informationhot-hugo}`.
- **콘텐츠 현황**: informationhot-hugo 536건 / kuta-hugo 2036건 / 5.informationhot-hugo 34건 — 기존 발행 콘텐츠와 slug 충돌 정책 미정.
- **wrangler Pages 프로젝트 목록**: 정보가 계열 4개 (informationhot-hugo/kuta-hugo/5-informationhot/info-techpawz) + 정보가 별도 프로젝트(informationhot, 도메인 미바인딩) 존재.
- **기존 Phase 36 실증 데이터**: chain 10015 (스파라쿠아), posts 10009~10011 (drafted) — 실발행 전까지 유지.

---

## Resume Instructions

```bash
# 1. 상태 확인
cd /Users/twinssn/projects2/mc
git log --oneline -3        # 기대: f63ecb1 (M2 등록), 716a665, de3bb43
git status --short          # 기대: clean (미푸시 f63ecb1 존재 — push 금지)

# 2. M2 조사 아카이브 확인 (선택)
cat .planning/phase-37/RESEARCH.md   # 필수 분리 지점 + 인프라 실측 + 미확정 5건

# 3. Phase 37 착수
/gsd:plan-phase 37
#    a. 복제 레포 생성 + 새 원격
#    b. .env 재생성
#    c. DB 분리 (chain_config db_path)
#    d. shared vendoring (ai_writer/env_loader/models.yaml, PATH_5000 제거)
#    e. R2 매핑 키 추가
```

**Resume note**: M2 (정보가 3-블로그 체인 복제, Phase 37~40) 등록 완료. 다음 세션은 **Phase 37 착수**. push/배포/실발행은 대표 최종 승인 후에만. Phase 36 실발행 게이트도 병행 대기 중.

---

## Uncommitted Changes

- **없음** — `git status --short` 결과 CLEAN.
- 미푸시 커밋 1건: `f63ecb1` (M2 마일스톤 등록) — push 생략 결정 (대표 승인 후 push 예정).
