# Continue Here — mc

**Created:** 2026-08-01 (갱신 2)
**Session focus:** Hugo repo 커밋 정리 + 표 렌더링 라이브 검증 + issue-techpawz 배포

---

## Current Position

- Phase 26~30 완료 (762 tests 기준선). Quick 20260801(표 렌더링 버그) 완료 + 배포 확인.
- **3개 Hugo repo 커밋 정리 완료** — 사전 미커밋 변경분 전부 카테고리별 커밋.
- **issue-techpawz-hugo 재배포 완료** — 알프스대영CC 표 렌더링 라이브 확인.
- 다음 우선순위: 카테고리 운영 계속 + P2/P3 백로그 (아래 Remaining Work).

---

## Completed This Session

### 1. 3개 Hugo Repo 카테고리별 커밋 (18건)

**issue-techpawz-hugo (6건):**
| Hash | 카테고리 | 메시지 |
|------|----------|--------|
| `46cc1df` | config | hugo.toml 설정 개편 + AdSense slot 갱신 |
| `a36d8c7` | layouts | AdSense partial 재작성 + top.html 신규 |
| `ca5bae4` | css | funnel-card.css 신규 |
| `1ab4621` | content | 썸네일 URL 주입 + HTML 태그 정리 + 표 수정 (265건) |
| `66d57dc` | layouts | extend_head.html GA4 + mobile CSS, _headers 캐시 규칙 |
| `7be94e4` | config | .gitignore 패턴 추가 + params.toml 설정 |

**techpawz-hugo (5건):**
| Hash | 카테고리 | 메시지 |
|------|----------|--------|
| `4f8996d4` | config | config/_default → 루트 hugo.toml로 마이그레이션 |
| `dd7391fd` | layouts | index.html 신규 + recent-articles + extend_head 리팩토링 |
| `d1eca71f` | css | custom.css 정리 — 버튼/썸네일 오버라이드 제거 |
| `d0b9004a` | content | CTA 중복 제거 + S3 포스트 정리 + 백업 파일 삭제 (29건) |
| `587a17e1` | config | .gitignore 패턴 추가 + _index.md |

**rotcha-blog (7건):**
| Hash | 카테고리 | 메시지 |
|------|----------|--------|
| `6ea7728` | config | [adsense] → [advertisement] 마이그레이션 + UI 토글 정리 |
| `528a48a` | layouts | AdSense partial 마이그레이션 + paragraph-splitting 제거 + dual-cta 단일화 |
| `1196e6d` | css | 링크 언더라인 스타일 추가 (light/dark mode) |
| `4f80876` | content | 2025 포스트 공백/줄 정리 (~850건) |
| `f6af61b` | content | 2026 포스트 trailing newline 제거 + S1 드래프트 삭제 (63건) |
| `f2650b7` | content | 나머지 포스트 공백 정리 + S1 드래프트 삭제 (724건) |
| `fb3a483` | config | .gitignore + 신규 S1 드래프트 + assets 이미지 |

### 2. 표 렌더링 라이브 검증

- **4개 사이트 전수 스캔**: 깨진 표 0건 확인 (issue-techpawz 287, techpawz 1520, rotcha 1333, informationhot 536 파일)
- **issue-techpawz-hugo 재배포** (`7ec955c3`): 알프스대영CC 표 `<table>` 렌더링 라이브 확인
- **webfetch 오탐 확인**: webfetch가 HTML→마크다운 변환 시 표 구조 뭉개짐 → curl + python3으로 HTML 직접 확인 후 정상 확정

### 3. 커밋 정리로 해소된 잔존 리스크

| 리스크 | 해소 내용 |
|--------|-----------|
| 3개 Hugo repo 사전 미커밋 변경분 | 18건 커밋으로 전부 해소 |
| .bak 파일 유출 | .gitignore에 *.bak 패턴 추가 |
| .DS_Store 유출 | .gitignore에 .DS_Store 패턴 추가 |
| public_test 빌드 아티팩트 | .gitignore에 public_test/ 패턴 추가 |

---

## Remaining Work

1. **(a) 40건 고아 이미지** — 재발행 전까지 이미지 없음
2. **Automotive 검색 개선** — search_retriever에 automotive-specific search template 필요
3. **Post #488 정리** — 테스트/플레이스홀더 포스트 삭제 시 10/10 checks 통과
4. **(P2) classify_priority 수정** — real_estate priority를 90으로 낮춰 도시명+부동산 키워드 정확 분류
5. **(P2) _parse_derivation 강화** — `json\n[...]` 패턴 지원 추가
6. **(P3) char_count config 조정** — step1~3 char_count 범위 상향
7. **(P3) depth 방향 프롬프트 확인** — stock/real_estate의 lateral 프롬프트 사용 확인
8. **(P3) pyproject include 정리** — 신규 루트 모듈 packages include 누락

---

## Decisions

| 의사결정 | 내용 |
|----------|------|
| 커밋 전략 | 카테고리별 분류 (config/layouts/css/content) — .bak, .DS_Store, public_test 제외 |
| 표 렌더링 검증 | curl + python3 HTML 직접 확인 (webfetch는 HTML→md 변환 시 표 구조 뭉개짐) |
| 배포 범위 | issue-techpawz-hugo만 배포 (표 수정 적용 필요했던 유일한 사이트) |
| rotcha S1 드래프트 | 신규 S1 드래프트 + assets 이미지一起 커밋 |

---

## Blockers

- (none)

---

## Bugs & Issues Discovered

### 1. [해결됨] issue-techpawz-hugo 라이브 표 미배포
- **증상**: 소스 파일(index.md)에 separator 행 존재하나 라이브에서 표가 텍스트로 렌더링
- **원인**: 이전 세션에서 표 수정 후 issue-techpawz-hugo 재배포하지 않음
- **해결책**: `hugo --gc --minify` + `wrangler pages deploy` 실행 → 라이브 `<table>` 렌더링 확인
- **미해결**: 아니오

### 2. [참고] webfetch HTML→마크다운 변환 표 구조 뭉개짐
- **증상**: webfetch markdown 형식으로 fetched 시 `<table>`이 `| col1 | col2 |` 텍스트로 변환
- **원인**: webfetch의 HTML→마크다운 변환기가 테이블 구조를 보존하지 못함
- **해결책**: curl + python3 regex로 HTML 직접 확인
- **미해결**: 아니오 (도구 한계)

---

## Environment Notes

- **Branch:** `feat/keyword-category-template`
- **Python:** `/Users/twinssn/.kaggle-env/bin/python` (pytest 실행 시 이 경로 사용)
- **DB Path:** `/Users/twinssn/Projects/5000/data/mc_chains.db`
- **pytest:** 762/762 (21.6s) — `test_markdown_processor.py` 33/33
- **Hugo sites:** `/Users/twinssn/Projects/{issue-techpawz-hugo, techpawz-hugo, rotcha-blog}`
  — 배포 시 `CLOUDFLARE_API_TOKEN` 등 env unset 후 hugh79757 프로필 사용
- **themesDir:** `/Users/twinssn/Projects/shared-themes` (Hugo 빌드 시 `HUGO_THEMESDIR` 필요)
- **AdSense:** rotcha 계열 6개 사이트 per-ad partial이 6677→8772 통일 필요 (CAP 미처리 항목)
- **issue-techpawz 배포:** `7ec955c3` (2026-08-01)

---

## Resume Instructions

```bash
cd /Users/twinssn/projects2/mc

# 1. 기준선 확인
/Users/twinssn/.kaggle-env/bin/python -m pytest --tb=short -q   # 762/762

# 2. 운영 계속 (새 키워드 → 3개 도메인 체인 발행)
python chain_publisher.py --seed "키워드"

# 3. 백로그 (Remaining Work 참고):
#    P2: classify_priority real_estate 90 · _parse_derivation json\n[...]
#    P3: char_count 상향 · pyproject include 정리

# 4. 배포 시 (필요 시):
cd /Users/twinssn/Projects/issue-techpawz-hugo && HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes hugo --gc --minify && env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./public --project-name issue-techpawz-hugo
```

---

## Uncommitted Changes (2026-08-01 갱신 기준)

**mc repo:**
- ` M README.md` (사전 존재분)
- `?? .planning/phase-28/CONTEXT.md` (사전 존재분)
- `?? .planning/phase-29/CONTEXT.md` (사전 존재분)
- `?? .planning/phases/phase-24/` (사전 존재분)
- `?? .planning/triage/20260730--fm-structural-fix-and-card-cta.md` (사전 존재분)

**Hugo repos:** 커밋 완료 — 깨끗 (rotcha .DS_Store 3건은 .gitignore 대상)
