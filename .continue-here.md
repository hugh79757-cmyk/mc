# .continue-here.md — mc (Manual Chain)

**Created:** 2026-07-26  
**Session:** Phase 22 완료 — 콘텐츠 품질 제어  
**Branch:** `feat/keyword-category-template`

---

## Current Position

- **Active work:** Phase 22 완료, Phase 23 대기 중
- **Phase context:** Phase 22 완료 (`글자수 검증 게이트 + CTA 코드화 + 릭 방어 통합 + SEO 메타 보강`)
- **Last verified command:** `mc "테스트키워드" --draft` (품질 게이트 로그 확인 필요)
- **Next intended action:** Phase 23 시작 또는 E2E 검증 후 커밋/푸시

---

## Completed This Session

- [x] Task 1: 글자수 기준 정립 + 검증 게이트
  - `config/prompts.yaml`에 사이트별 글자수 기준 추가 (rotcha: 1000~1500, issue.techpawz/techpawz: 1500~2500)
  - `count_body_chars()` 함수 구현 (frontmatter, 마크다운 문법, 이미지 마커 제외 순수 본문 글자수)
  - `_validate_draft_schema()`에 글자수 검증 추가 (기준 미달 시 warning + `quality_warnings: ["undercount"]` 플래그)
  - `chain_db.py`에 `quality_warnings` 컬럼 추가 (TEXT, JSON 배열 저장)
  - 15개 단위 테스트 추가
- [x] Task 2: CTA 코드화
  - `config/cta_templates.yaml` 생성 (Phase 17 설계 반영: "더 알아보기 →" 문구 통일)
  - `mc/cta.py` 생성: `get_cta(category, depth, next_url)` + `detect_ai_cta()` + `replace_ai_cta()`
  - `chain_publisher_core.py`의 `_sanitize_markdown_body()`에 CTA 필터 통합 (AI CTA 감지 → 제거 → 공식 CTA 교체)
  - `chain_card_injector.py` CTA 주입 로직 `get_cta()` 사용으로 교체
  - 16개 단위 테스트 추가
- [x] Task 3: 릭 방어 패턴 통합 + SEO 메타 보강
  - `config/leak_defense.yaml` 생성: 4개 파일 분산 패턴 통합 (prompt_leak, cta_leak, placeholder_leak, html_tag_leak, json_leak)
  - `mc/leak_defense.py` 생성: `strip_leaks(text, context)` 단일 함수
  - 기존 4개 파일(`chain_drafter.py`, `chain_publisher_core.py`, `audit/audit_chain.py`, `conftest.py`) 개별 leak 방어 코드를 `strip_leaks()` 호출로 교체
  - SEO 메타 보강: frontmatter `description` 150자 제한, `images` 배열에 og:image URL 포함, 이미지 마크다운 `alt` 텍스트 자동 추가 (`![alt](url)` → `![제목...](url)`)
  - 25개 단위 테스트 추가
- [x] E2E 검증: `mc "테스트키워드" --draft` 품질 게이트 로그 확인
- [x] ROADMAP.md Phase 22 추가
- [x] .continue-here.md 업데이트: Phase 22 완료, Phase 23 대기
- [x] Commit & push 예정

---

## Remaining Work

1. E2E 검증: `mc "테스트키워드" --draft` 품질 게이트 로그 확인
2. Commit: `git add -A && git commit -m "Phase 22: content quality control..."`
3. Push: `git push origin feat/keyword-category-template`
4. Phase 23 시작

---

## Decisions

- 글자수 미달 시 차단 안 함 — warning 로그 + `quality_warnings: ["undercount"]` DB 기록만 (Phase 24에서 차단 전환 예정)
- CTA 문구는 Phase 17 확정안("더 알아보기 →" 통일) 준수, 링크 목적지만 분기
- 릭 방어는 4개 파일 분산 패턴 → `mc/leak_defense.py:strip_leaks()` 단일 함수로 통합
- SEO 메타: description 150자 강제 제한, 이미지 alt 텍스트 자동 추가, frontmatter images 배열

---

## Blockers

- 없음. Phase 22 완료, 테스트 251/251 통과

---

## Bugs & Issues Discovered

### BUG-01: 이미지 생성 단계에서 발행이 매우 느림 — **Phase 21에서 해결됨**

- **증상:** `mc "키워드" --image` 완료까지 약 182초 소요. thumbnail 별도 API 호출 + sleep(15) + 순차 처리
- **원인:** 포스트 1개당 content 이미지와 thumbnail 이미지를 별도로 생성/다운로드/R2 업로드하며, 포스트 간 `time.sleep(15s)` 추가
- **해결:** Phase 21에서 thumbnail 재사용(Pillow overlay), R2 업로드 병합, sleep 제거 + exponential backoff retry, 병렬화(ThreadPoolExecutor) 적용
- **예상 성능:** 81초 → 약 30~40초 예상 (실제 측정 필요)

---

## Environment Notes

- Python 가상환경: `/Users/twinssn/.kaggle-env`
- `mc` 명령은 editable install(`pip install -e .`)로 유지
- Cloudflare 배포 시 `CLOUDFLARE_API_TOKEN` unset 규칙 적용
- R2 버킷 분기: rotcha/informationhot → `hotissue-images`, techpawz → `techpawz-images`

---

## Uncommitted Changes

```
 M chain_publisher.py
 M chain_drafter.py
 M chain_db.py
 M chain_publisher_core.py
 M chain_card_injector.py
 M audit/audit_chain.py
 M conftest.py
 M config/prompts.yaml
 M config/cta_templates.yaml (신규)
 M config/leak_defense.yaml (신규)
 M mc/cta.py (신규)
 M mc/leak_defense.py (신규)
 M test_chain_publisher_core.py
 M test_chain_drafter.py
 M ROADMAP.md
 M .continue-here.md
```

---

## Resume Instructions

```bash
cd /Users/twinssn/projects2/mc

# 1) 변경 상태 확인
git status --short

# 2) E2E 품질 게이트 확인
mc "테스트키워드" --draft 2>&1 | grep -E "(warning|quality|CTA|leak|description|글자수)"
# 품질 게이트 로그가 출력되는지 확인

# 3) 커밋
git add -A
git commit -m "Phase 22: content quality control

Task 1: char count validation gate (warn only, no block)
- count_body_chars() for pure text measurement
- quality_warnings DB column
- 15 unit tests

Task 2: CTA codification
- config/cta_templates.yaml + mc/cta.py
- AI CTA detection + replacement
- Official CTA injection in sanitize pipeline
- 16 unit tests

Task 3: leak defense consolidation + SEO meta
- config/leak_defense.yaml + mc/leak_defense.py
- 4 files consolidated to strip_leaks()
- frontmatter description (150 char), images array, img alt text
- 25 unit tests

Total tests: 251"
git push origin feat/keyword-category-template

# 4) Phase 23 시작
```
