# Continue Here — mc

**Created:** 2026-08-01 (갱신)
**Session focus:** Quick 20260801 — 마크다운 표 separator 누락 렌더링 버그 수정 + 재발 방지 + 커밋

---

## Current Position

- Phase 26~30 완료 (764→762 tests 기준선). Quick 20260801(표 렌더링 버그) **완료·커밋됨**.
- 발행 파이프라인에 `fix_tables()` 자동 보정이 포함되어 운영 중.
- 다음 우선순위: 카테고리 운영 계속 + P2/P3 백로그 (아래 Remaining Work).

---

## Completed This Session

1. **근본 원인 확정**: AI 초안(`output/drafts/296/step-2-…s2.md`)이 마크다운 표의 header 다음
   separator 행(`| --- | --- |`)을 누락 → Hugo goldmark가 `<table>` 아닌 `<p>`로 렌더링.
2. **전수 스캔 (4개 사이트, 3,676 md)**: 실제 깨진 표 9건 확정.
   초기 rough 스캔 48/24/217건은 오탐(정상 header+separator 표를 오판) → 스캐너 재작성.
3. **9건 수정** (사용자 결정: 전 사이트 수정 + 잘린 빈 표 5건은 제거):
   - issue-techpawz-hugo 4건: 알프스대영cc(separator 삽입), 농지대장(Q&A→유효 표),
     무직자/건강보험(잘린 빈 표 제거)
   - techpawz-hugo 1건: 프리랜서-고유가(잘린 빈 표 제거)
   - rotcha-blog 4건: 설비보전/건축도장(mangled header 복원), 고유가-인천/고물가(빈 표 제거)
4. **재발 방지 3중**:
   - `markdown_processor.py` `fix_tables()` 신설 — separator 삽입/빈 표 제거/stray pipe 제거,
     코드 펜스 보호, `clean_symbols()` 진입 시 자동 적용 (Hugo/Blogger 발행 경로 모두 커버)
   - `config/prompts.yaml` — 표 3줄 구조(header → `| --- | --- |` → 데이터) 지시 추가
   - `test_markdown_processor.py` TestFixTables 8건 신설
5. **검증**: pytest **762/762** (754 기존 + 8 신규) · 3개 사이트 Hugo 재빌드 성공 ·
   수정 4건 `<table>` 렌더링 확인 · 제거 5건 잔존 파이프 0건 · 4개 사이트 재스캔 0 broken ·
   파이프라인 e2e(실제 알프스대영 초안 → separator 자동 삽입) 확인.
6. **산출물**: `.planning/quick/20260801-broken-table-render/SUMMARY.md` 작성, `.planning/STATE.md` 갱신.
7. **커밋 3건**:
   - mc repo `2397636` (fix_tables + prompts + 테스트 + quick/STATE.md, 6 files)
   - issue-techpawz-hugo `ab73c54` (표 4건, 4 files)
   - techpawz-hugo `ee65d25f` (표 1건, 1 file)
   - rotcha-blog: 변경 0건 — **이미 커밋 상태** (08ee6b3에 포함 확인)

---

## Remaining Work

1. **(a) 40건 고아 이미지** — 재발행 전까지 이미지 없음
2. **Automotive 검색 개선** — search_retriever에 automotive-specific search template 필요
3. **Post #488 정리** — 테스트/플레이스홀더 포스트 삭제 시 10/10 checks 통과
4. **(P2) classify_priority 수정** — real_estate priority를 90으로 낮춰 도시명+부동산 키워드 정확 분류
5. **(P2) _parse_derivation 강화** — `json\n[...]` 패턴 지원 추가
6. **(P3) char_count config 조정** — step1~3 char_count 범위 상향
7. **(P3) depth 방향 프롬프트 확인** — stock/real_estate의 lateral 프롬프트 사용 확인
8. **(P3) pyproject include 정리** — 신규 루트 모듈(frontmatter_utils/url_utils/constants/
   link_finder/card_generator/html_renderer/markdown_processor) packages include 누락

---

## Decisions

| 의사결정 | 내용 |
|----------|------|
| 수정 범위 | 전 사이트 수정 (3개 사이트 9건) + 데이터 잘린 빈 표 5건은 제거 |
| 재발 방지 | 파이프라인 자동 보정(`fix_tables`) + 프롬프트 3줄 구조 지시 + 테스트 8건 = 3중 |
| 스캔 판정 | "연속 \| 행 run 두 번째 줄이 separator 아니면 깨진 표" + "header 단독 잘린 표(bare \|)" — header+separator 정상 표는 오탐 방지 |
| 커밋 범위 | 이번 작업 파일만 선택 커밋 — 사전 존재 변경분(README.md, .planning 파일, Hugo repo 기타 변경)은 건드리지 않음 |
| rotcha 처리 | git status 변경 0건 → 커밋 생략 (이미 커밋 상태) |

---

## Blockers

- (none) — 이번 세션 작업은 모두 완료.

---

## Bugs & Issues Discovered

### 1. [해결됨] 마크다운 표 separator 누락 → 렌더링 깨짐
- **증상**: issue.techpawz.com 알프스대영CC 포스트에서 표가 텍스트로 나옴
- **원인**: AI 초안이 header 다음 separator 행 누락 → goldmark가 `<table>` 아닌 `<p>` 렌더링
- **해결책**: 9건 수동 수정 + `markdown_processor.fix_tables()` 파이프라인 자동 보정 +
  prompts.yaml 3줄 구조 지시
- **미해결**: 아니오 (해결됨) — 단 header 완전 분실된 mangled 표(`---|…|` + 데이터 행)는
  `fix_tables`가 복원 불가(header 추론 불가) → 프롬프트 지시로 예방 중. **잔존 위험**

### 2. [해결됨] 초기 전수 스캔 오탐 (48/24/217건)
- **증상**: rough 스캔이 정상 표를 대량 오탐
- **원인**: header+separator가 정상인 표를 "데이터 행 연속"으로 오판하는 판정 로직
- **해결책**: 판정 로직 재작성 (separator 유무 기준) → 9건 확정, 0건 재스캔
- **미해결**: 아니오

### 3. [미해결 — 다음 세션 참고] 3개 Hugo repo 사전 미커밋 변경분 다수
- **증상**: issue-techpawz(265건)/techpawz(2건)/rotcha(909건) content·config 변경 잔존
- **원인**: 이번 작업과 무관한 사전 변경분 (custom.css, config/*.toml, 수십 개 포스트 등)
- **해결책**: 이번 작업에서는 제외. 사용자가 별도 정리 필요
- **미해결**: 예 — 커밋 범위 확정 필요

---

## Environment Notes

- **Branch:** `feat/keyword-category-template`
- **Python:** `/Users/twinssn/.kaggle-env/bin/python` (pytest 실행 시 이 경로 사용)
- **DB Path:** `/Users/twinssn/Projects/5000/data/mc_chains.db`
- **pytest:** 762/762 (21.6s) — `test_markdown_processor.py` 33/33
- **Hugo sites:** `/Users/twinssn/Projects/{issue-techpawz-hugo, techpawz-hugo, rotcha-blog}`
  — 배포 시 `CLOUDFLARE_API_TOKEN` 등 env unset 후 hugh79757 프로필 사용
- **themesDir:** `/Users/twinssn/Projects/shared-themes` (Hugo 빌드 시 `HUGO_THEMESDIR` 필요)
- **스캔 스크립트:** `/var/folders/6r/kjl8wkw53t1bnr1dypqtccj80000gn/T/opencode/scan_broken_tables.py`
  (임시 — 보존 가치 낮음, 재사용 시 복사 필요)
- **AdSense:** rotcha 계열 6개 사이트 per-ad partial이 6677→8772 통일 필요 (CAP 미처리 항목)

---

## Resume Instructions

```bash
cd /Users/twinssn/projects2/mc

# 1. 기준선 확인
/Users/twinssn/.kaggle-env/bin/python -m pytest --tb=short -q   # 762/762

# 2. 운영 계속 (새 키워드 → 3개 도메인 체인 발행)
python chain_publisher.py --seed "키워드"

# 3. 깨진 표 재스캔 (재발 확인용 — 스크립트가 임시 경로라면 복사 후 실행)
#    4개 사이트 content/posts 대상, 0건 기대

# 4. 백로그 (Remaining Work 참고):
#    P2: classify_priority real_estate 90 · _parse_derivation json\n[...]
#    P3: char_count 상향 · pyproject include 정리
```

---

## Uncommitted Changes (2026-08-01 기준)

**mc repo (사전 존재분 — 이번 작업 범위 밖, 커밋 안 함):**
- ` M README.md`
- `?? .planning/phase-28/CONTEXT.md`
- `?? .planning/phase-29/CONTEXT.md`
- `?? .planning/phases/phase-24/`
- `?? .planning/triage/20260730--fm-structural-fix-and-card-cta.md`

**Hugo repos (사전 존재분 — 커밋 안 함):**
- issue-techpawz-hugo: content/posts 265건 + custom.css + config (이번 작업 4건은 ab73c54에 커밋됨)
- techpawz-hugo: content/posts 2건 + custom.css + config 삭제분 (이번 작업 1건은 ee65d25f에 커밋됨)
- rotcha-blog: content/posts 909건 + .DS_Store + config (이번 작업분은 이미 커밋 상태)
