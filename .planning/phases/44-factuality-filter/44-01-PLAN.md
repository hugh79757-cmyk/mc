---
phase: 44-factuality-filter
plan: "01"
type: execute
wave: 1
depends_on: ["43-01"]
files_modified:
  - quality/factuality_checker.py
  - tests/test_factuality_checker.py
autonomous: true
requirements: ["QG-05", "QG-06"]
must_haves:
  truths:
    - "'트립어드바이저 리뷰 데이터를 분석하면' 같은 무출처 데이터 주장이 violation으로 탐지된다"
    - "'오전 방문객 밀집도 73%' 같은 출처 없는 수치가 차단된다"
    - "[출처: 남양주시청, 2026-08-01] 태그가 있는 수치는 통과한다"
  artifacts:
    - path: "quality/factuality_checker.py"
      provides: "무출처 수치/후기/통계 탐지 + 금지 표현 검사"
      min_lines: 80
  key_links:
    - from: "quality/factuality_checker.py"
      to: "quality/contract_loader.py"
      via: "ContractSpec.forbidden_patterns를 금지 표현 검사에 사용"
---

<objective>
본문에서 출처 없는 수치·통계·후기 데이터를 탐지하고,
금지 표현을 검사하여 발행 전 차단하는 사실성 필터를 구현한다.
</objective>

<context>
@quality/contract_loader.py
@quality/pre_researcher.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: factuality_checker.py 구현</name>
  <files>quality/factuality_checker.py</files>
  <read_first>quality/contract_loader.py, quality/pre_researcher.py</read_first>
  <action>
  구현:
  - extract_claims(body_md: str) -> list[Claim]:
    수치 패턴(N%, N명, N만원, N점, 만족도 N), 후기/리뷰 언급,
    통계 주장("데이터를 분석하면", "설문 결과") 문장을 추출.
    Claim(sentence: str, claim_type: str, has_source_tag: bool)
  - check_forbidden(body_md: str, patterns: list[str]) -> list[str]:
    계약서의 forbidden_patterns 매칭. 위반 문장 리스트 반환.
  - validate_factuality(body_md: str, contract: ContractSpec) -> FactualityResult:
    FactualityResult(score: float, unsourced_claims: list, forbidden_hits: list, passed: bool)
    score = 출처있는claims / 전체claims. 0.7 미만이면 passed=False.
    forbidden_hits가 1개라도 있으면 passed=False.
  </action>
  <verify>python -c "from quality.factuality_checker import extract_claims; claims = extract_claims('방문객 만족도 73%입니다.'); print(len(claims), claims[0].has_source_tag)"</verify>
  <acceptance_criteria>
    - "방문객 만족도 73%" → has_source_tag=False인 Claim 탐지
    - "방문객 만족도 73% [출처: 남양주시청]" → has_source_tag=True
    - forbidden 패턴 "최저가" 포함 문장 → forbidden_hits에 추가
  </acceptance_criteria>
  <done>사실성 필터 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: factuality_checker 테스트</name>
  <files>tests/test_factuality_checker.py</files>
  <read_first>quality/factuality_checker.py</read_first>
  <action>
  - test_extract_percentage_claim: "73%" 문장 탐지
  - test_extract_review_claim: "리뷰 데이터를 분석하면" 탐지
  - test_sourced_claim_passes: [출처:] 태그 있으면 has_source_tag=True
  - test_forbidden_pattern_hit: "최저가" → forbidden_hits
  - test_score_below_threshold: 출처 없는 claims 비율 높으면 passed=False
  - test_clean_post_passes: 수치 없거나 전부 출처 있으면 passed=True
  - test_real_techpawz_fixture: 남양주 techpawz 삭제 원본으로 검증 (실제 환각 탐지 확인)
  </action>
  <verify>cd /Users/twinssn/projects2/mc && python -m pytest tests/test_factuality_checker.py -v</verify>
  <acceptance_criteria>
    - 7개 테스트 전체 통과
    - fixture 테스트에서 techpawz 글의 무출처 수치가 실제로 탐지됨
  </acceptance_criteria>
  <done>사실성 필터 테스트 완료</done>
</task>
</tasks>