---
phase: 45-cross-blog-dedup
plan: "01"
type: execute
wave: 1
depends_on: ["41-01"]
files_modified:
  - quality/cross_blog_checker.py
  - tests/test_cross_blog_checker.py
autonomous: true
requirements: ["QG-07", "QG-08"]
must_haves:
  truths:
    - "동일 chain 3편에서 문장 유사도 30% 초과 쌍이 탐지되면 violation"
    - "rotcha 글에 비교표가 있으면 역할 위반으로 탐지된다"
    - "issue 글에 비교표가 없으면 역할 필수 요소 누락으로 탐지된다"
  artifacts:
    - path: "quality/cross_blog_checker.py"
      provides: "3편 문장 중복 + 역할 필수 요소 검증"
      min_lines: 100
  key_links:
    - from: "quality/cross_blog_checker.py"
      to: "quality/contract_loader.py"
      via: "ContractSpec.required_sections로 역할별 필수 요소 확인"
---

<objective>
동일 chain의 3개 블로그 글을 비교하여 문장 중복을 탐지하고,
각 블로그가 자기 역할에 맞는 필수 요소를 포함하는지 검증한다.
</objective>

<context>
@quality/contract_loader.py
@contracts/rotcha.yaml
@contracts/issue_techpawz.yaml
@contracts/techpawz.yaml
</context>

<tasks>
<task type="auto">
  <name>Task 1: cross_blog_checker.py 구현</name>
  <files>quality/cross_blog_checker.py</files>
  <read_first>quality/contract_loader.py</read_first>
  <action>
  구현:
  - split_sentences(body_md: str) -> list[str]: 마크다운 본문을 문장 단위 분리
  - calc_similarity(sents_a: list, sents_b: list) -> float:
    n-gram(3-gram) 기반 Jaccard 유사도. 외부 라이브러리 없이 구현.
  - check_cross_blog_dedup(posts: dict[str, str]) -> DedupResult:
    posts = {"rotcha": body_md, "issue_techpawz": body_md, "techpawz": body_md}
    3쌍 비교. 유사도 0.30 초과 쌍 → violations에 추가.
    DedupResult(passed: bool, pair_scores: dict, violations: list)
  - check_role_elements(blog_id: str, body_md: str, contract: ContractSpec) -> RoleResult:
    required_sections 존재 여부 검사.
    RoleResult(passed: bool, missing_sections: list, extra_sections: list)
  </action>
  <verify>python -c "from quality.cross_blog_checker import calc_similarity; print(calc_similarity(['동일 문장입니다']*3, ['동일 문장입니다']*3))"</verify>
  <acceptance_criteria>
    - 동일 문장 리스트 비교 시 유사도 1.0
    - 완전히 다른 문장 비교 시 유사도 0.1 미만
    - required_sections 누락 시 missing_sections에 포함
  </acceptance_criteria>
  <done>크로스 블로그 중복·역할 검증 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: cross_blog_checker 테스트</name>
  <files>tests/test_cross_blog_checker.py</files>
  <read_first>quality/cross_blog_checker.py</read_first>
  <action>
  - test_identical_posts_fail: 동일 텍스트 3편 → 유사도 1.0, passed=False
  - test_unique_posts_pass: 완전히 다른 3편 → passed=True
  - test_partial_overlap: 30% 경계 테스트
  - test_role_check_rotcha_pass: rotcha 필수 섹션 모두 포함 → passed=True
  - test_role_check_issue_missing_comparison: issue에 비교표 없음 → missing에 포함
  - test_real_남양주_fixture: 삭제 원본 3편으로 실제 중복도 측정
  </action>
  <verify>cd /Users/twinssn/projects2/mc && python -m pytest tests/test_cross_blog_checker.py -v</verify>
  <acceptance_criteria>
    - 6개 테스트 전체 통과
    - fixture 테스트에서 남양주 3편의 실제 유사도가 0.30 초과
  </acceptance_criteria>
  <done>크로스 블로그 검증 테스트 완료</done>
</task>
</tasks>