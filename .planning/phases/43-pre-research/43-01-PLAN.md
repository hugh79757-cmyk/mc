---
phase: 43-pre-research
plan: "01"
type: execute
wave: 1
depends_on: ["41-01"]
files_modified:
  - quality/pre_researcher.py
  - tests/test_pre_researcher.py
autonomous: true
requirements: ["QG-04", "QG-05"]
must_haves:
  truths:
    - "키워드 입력 시 웹 검색 결과에서 공식 사이트 URL, 주소, 운영시간, 요금 등을 추출한 factsheet가 생성된다"
    - "factsheet의 각 fact 항목에 source_url과 retrieved_date가 포함된다"
    - "검색 실패 시 빈 factsheet를 반환하고 파이프라인을 중단하지 않는다"
  artifacts:
    - path: "quality/pre_researcher.py"
      provides: "키워드 기반 웹 리서치 → factsheet 생성"
      min_lines: 100
  key_links:
    - from: "quality/pre_researcher.py"
      to: "shared/ai_writer.py"
      via: "draft 호출 전에 factsheet를 prompt context로 주입"
---

<objective>
키워드를 받아 웹 검색으로 공식 정보를 수집하고, 출처 추적 가능한
factsheet.json을 생성하는 리서치 스텝을 파이프라인에 삽입한다.
derive 이후, draft 이전에 실행된다.
</objective>

<context>
@shared/ai_writer.py
@config/chain_config.yaml
</context>

<tasks>
<task type="auto">
  <name>Task 1: pre_researcher.py 구현</name>
  <files>quality/pre_researcher.py</files>
  <read_first>shared/ai_writer.py, config/chain_config.yaml</read_first>
  <action>
  구현:
  - research_keyword(keyword: str) -> Factsheet:
    1. Naver Search API (기존 search_retriever.py 재사용) 또는 fallback 검색으로 상위 5개 결과 수집
    2. 각 결과 페이지에서 주요 사실 추출: 주소, 운영시간, 요금, 교통편, 공식 URL
    3. Factsheet dataclass: facts: list[Fact], keyword, researched_at
    4. Fact dataclass: claim: str, source_url: str, retrieved_date: str, confidence: str
  - save_factsheet(factsheet: Factsheet, chain_id: str) -> Path:
    data/factsheets/{chain_id}.json으로 저장
  - 검색 실패/타임아웃 시: 빈 Factsheet 반환, 로그 기록, 예외 발생 안 함
  </action>
  <verify>python -c "from quality.pre_researcher import Factsheet, Fact; f = Factsheet(facts=[], keyword='test', researched_at='2026-08-18'); print(f)"</verify>
  <acceptance_criteria>
    - Factsheet, Fact dataclass import 가능
    - research_keyword가 Factsheet 반환
    - 검색 실패 시 빈 facts 리스트 + 예외 없음
  </acceptance_criteria>
  <done>리서치 모듈 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: 파이프라인 삽입 포인트 구현</name>
  <files>quality/pre_researcher.py</files>
  <read_first>shared/ai_writer.py, cli/mc.py</read_first>
  <action>
  - inject_factsheet_to_prompt(factsheet: Factsheet, base_prompt: str) -> str:
    factsheet의 facts를 "## 사전 리서치 결과" 섹션으로 포맷팅하여 prompt 앞에 삽입.
    각 fact에 "[출처: {source_url}, {retrieved_date}]" 포함.
    facts가 비어있으면 "사전 리서치 결과 없음. 확인되지 않은 수치/통계를 사용하지 마시오." 경고 삽입.
  - CLI 파이프라인의 derive→draft 사이 호출 지점은 Phase 47에서 통합 (이 Phase에서는 모듈만 구현)
  </action>
  <verify>python -c "from quality.pre_researcher import inject_factsheet_to_prompt, Factsheet; print(inject_factsheet_to_prompt(Factsheet(facts=[], keyword='t', researched_at='now'), 'base')[:50])"</verify>
  <acceptance_criteria>
    - inject_factsheet_to_prompt가 빈 factsheet에 경고 문구 삽입
    - facts 있을 때 "[출처:" 태그 포함된 섹션 생성
  </acceptance_criteria>
  <done>팩트시트 프롬프트 주입 함수 구현 완료</done>
</task>

<task type="auto">
  <name>Task 3: pre_researcher 테스트</name>
  <files>tests/test_pre_researcher.py</files>
  <read_first>quality/pre_researcher.py</read_first>
  <action>
  - test_factsheet_dataclass: Factsheet/Fact 생성 및 직렬화
  - test_inject_empty_factsheet: 빈 facts → 경고 문구 포함
  - test_inject_with_facts: facts 있을 때 출처 태그 포함
  - test_research_keyword_mock: search API mock → Factsheet 반환
  - test_research_keyword_failure: 검색 실패 mock → 빈 Factsheet, 예외 없음
  </action>
  <verify>cd /Users/twinssn/projects2/mc && python -m pytest tests/test_pre_researcher.py -v</verify>
  <acceptance_criteria>
    - 5개 테스트 전체 통과
    - 기존 테스트 영향 없음
  </acceptance_criteria>
  <done>리서치 모듈 테스트 완료</done>
</task>
</tasks>