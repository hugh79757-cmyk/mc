---
phase: 42-title-body-check
plan: "01"
type: execute
wave: 1
depends_on: ["41-01"]
files_modified:
  - quality/title_body_checker.py
  - tests/test_title_body_checker.py
autonomous: true
requirements: ["QG-03"]
must_haves:
  truths:
    - "제목 '양귀비 개화시기 및 산책코스'에서 ['개화시기', '산책코스'] 토픽이 추출된다"
    - "본문에 '산책코스' H2가 없으면 TitleBodyResult.unmet_promises에 '산책코스'가 포함된다"
    - "모든 약속 충족 시 score >= 0.8"
  artifacts:
    - path: "quality/title_body_checker.py"
      provides: "제목-본문 정합성 검증"
      min_lines: 80
    - path: "tests/test_title_body_checker.py"
      provides: "정합성 검증 테스트"
      min_lines: 50
  key_links:
    - from: "quality/title_body_checker.py"
      to: "quality/contract_loader.py"
      via: "ContractSpec.required_sections와 제목 토픽 교차 검증"
---

<objective>
제목에서 핵심 약속 토픽을 추출하고, 본문 H2/H3 섹션에 해당 토픽이
구체적 정보와 함께 다뤄졌는지 자동 검증하는 모듈을 구현한다.
</objective>

<context>
@quality/contract_loader.py
@contracts/rotcha.yaml
</context>

<tasks>
<task type="auto">
  <name>Task 1: title_body_checker.py 구현</name>
  <files>quality/title_body_checker.py</files>
  <read_first>quality/contract_loader.py</read_first>
  <action>
  구현할 함수:
  - extract_title_promises(title: str) -> list[str]:
    조사·접속사 제거 후 핵심 명사구 토픽 추출.
    "및", "과", ",", "+" 등을 분리자로 사용.
  - check_section_coverage(body_md: str, promises: list[str]) -> dict:
    본문을 H2/H3 기준으로 섹션 분리.
    각 promise에 대해 해당 토픽을 다루는 섹션 존재 여부 판정.
    섹션 내 구체 정보(고유명사, 수치, 주소 등) 1개 이상 포함 여부 확인.
  - validate_title_body(title: str, body_md: str) -> TitleBodyResult:
    TitleBodyResult(score: float, met_promises: list, unmet_promises: list, details: dict)
  </action>
  <verify>python -c "from quality.title_body_checker import extract_title_promises; print(extract_title_promises('양귀비 개화시기 및 산책코스'))"</verify>
  <acceptance_criteria>
    - extract_title_promises('양귀비 개화시기 및 산책코스') → ['개화시기', '산책코스'] 포함
    - 본문에 해당 섹션 없을 때 unmet_promises에 추가됨
    - 전체 충족 시 score >= 0.8
  </acceptance_criteria>
  <done>제목-본문 정합성 검증 모듈 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: title_body_checker 테스트</name>
  <files>tests/test_title_body_checker.py</files>
  <read_first>quality/title_body_checker.py</read_first>
  <action>
  pytest 테스트:
  - test_extract_promises_compound_title: "A 및 B" → ['A', 'B']
  - test_extract_promises_single: "남양주 물의 정원" → ['남양주 물의 정원']
  - test_coverage_all_met: 모든 promise에 대응하는 H2 + 구체 정보 → score >= 0.8
  - test_coverage_partial: 1개 promise 누락 → unmet_promises에 포함, score < 0.8
  - test_coverage_empty_section: H2는 있지만 내용이 일반론만 → unmet으로 판정
  - test_real_post_남양주: 실제 남양주 물의 정원 삭제 원본으로 검증 (fixture)
  </action>
  <verify>cd /Users/twinssn/projects2/mc && python -m pytest tests/test_title_body_checker.py -v</verify>
  <acceptance_criteria>
    - 6개 이상 테스트 전체 통과
    - 남양주 fixture 테스트에서 기존 글의 unmet_promises가 비어있지 않음 (실제 결함 탐지 확인)
  </acceptance_criteria>
  <done>정합성 검증 테스트 통과</done>
</task>
</tasks>