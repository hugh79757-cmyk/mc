---
phase: 46-html-render-check
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/html_render_checker.py
  - tests/test_html_render_checker.py
autonomous: true
requirements: ["QG-09"]
must_haves:
  truths:
    - "HTML title 태그와 본문 첫 H1이 동일 문자열이면 duplicate_title violation"
    - "CTA 패턴이 2회 이상 나타나면 duplicate_cta violation"
    - "정상 HTML은 violations 빈 리스트로 통과"
  artifacts:
    - path: "quality/html_render_checker.py"
      provides: "Hugo 빌드 HTML 중복 검사"
      min_lines: 60
  key_links:
    - from: "quality/html_render_checker.py"
      to: "quality/contract_loader.py"
      via: "ContractSpec.cta_max_count로 CTA 반복 임계값 참조"
---

<objective>
Hugo 빌드 후 최종 HTML을 파싱하여 제목 중복, CTA 반복,
동일 문단 반복을 자동 탐지하는 post-build 검사를 구현한다.
</objective>

<context>
@quality/contract_loader.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: html_render_checker.py 구현</name>
  <files>quality/html_render_checker.py</files>
  <read_first>quality/contract_loader.py</read_first>
  <action>
  from html.parser import HTMLParser (표준 라이브러리만 사용, BeautifulSoup 의존 없이).
  구현:
  - parse_html(html_str: str) -> ParsedPage:
    title 태그, H1/H2 목록, CTA 패턴 목록, 문단 텍스트 목록 추출.
    CTA 패턴: "다음 글", "관련 글", "함께 보면", href 포함 블록.
  - check_html_duplicates(html_str: str, contract: ContractSpec) -> HtmlCheckResult:
    1. title == 첫 H1 → duplicate_title violation
    2. CTA 패턴 count > contract.cta_max_count → duplicate_cta violation
    3. 동일 문단 텍스트 2회 이상 → duplicate_paragraph violation
    HtmlCheckResult(passed: bool, violations: list[str])
  </action>
  <verify>python -c "from quality.html_render_checker import check_html_duplicates; from quality.contract_loader import load; print(check_html_duplicates('<html><title>T</title><body><h1>T</h1></body></html>', load('rotcha')))"</verify>
  <acceptance_criteria>
    - title과 H1 동일 시 duplicate_title 탐지
    - CTA 2회 반복 시 duplicate_cta 탐지
    - 정상 HTML은 passed=True
  </acceptance_criteria>
  <done>HTML 중복 검사 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: html_render_checker 테스트</name>
  <files>tests/test_html_render_checker.py</files>
  <read_first>quality/html_render_checker.py</read_first>
  <action>
  - test_duplicate_title: title == H1 → violation
  - test_no_duplicate_title: title != H1 → pass
  - test_duplicate_cta: CTA 3회 → violation
  - test_single_cta: CTA 1회 → pass
  - test_duplicate_paragraph: 동일 문단 2회 → violation
  - test_clean_html: 정상 → passed=True, violations=[]
  </action>
  <verify>cd /Users/twinssn/projects2/mc && python -m pytest tests/test_html_render_checker.py -v</verify>
  <acceptance_criteria>
    - 6개 테스트 전체 통과
  </acceptance_criteria>
  <done>HTML 중복 검사 테스트 완료</done>
</task>
</tasks>