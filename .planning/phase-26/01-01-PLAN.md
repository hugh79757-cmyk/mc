---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: [phase-22]
files_modified:
  - chain_drafter.py
  - chain_publisher_core.py
files_created:
  - frontmatter_utils.py
autonomous: true
requirements:
  - R-REF-01
  - R-REF-02
  - R-REF-03
user_setup: []

must_haves:
  truths:
    - "Frontmatter is correctly processed for all articles without duplication errors"
    - "Frontmatter handling works consistently across drafter and publisher modules"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "frontmatter_utils.py"
      provides: "Centralized frontmatter handling with ensure_frontmatter function"
      min_lines: 15
    - path: "chain_drafter.py"
      provides: "Updated to use frontmatter_utils for frontmatter handling"
      exports: ["ensure_frontmatter"]
    - path: "chain_publisher_core.py"
      provides: "Updated to use frontmatter_utils for frontmatter handling"
      exports: ["ensure_frontmatter"]
  key_links:
    - from: "chain_drafter.py"
      to: "frontmatter_utils.py"
      via: "import frontmatter_utils"
      pattern: "from frontmatter_utils import ensure_frontmatter"
    - from: "chain_publisher_core.py"
      to: "frontmatter_utils.py"
      via: "import frontmatter_utils"
      pattern: "from frontmatter_utils import ensure_frontmatter"

<objective>
Create frontmatter utility module to eliminate duplicate frontmatter handling functions.

Purpose: Replace duplicate _ensure_frontmatter and _ensure_frontmatter_closer functions with a single ensure_frontmatter function in a dedicated utility module
Output: frontmatter_utils.py module and updated imports in chain_drafter.py and chain_publisher_core.py
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontmatter_utils.py with unified ensure_frontmatter function</name>
  <files>frontmatter_utils.py</files>
  <action>Create frontmatter_utils.py with a single ensure_frontmatter(text, meta) function that replaces the duplicate _ensure_frontmatter and _ensure_frontmatter_closer functions. The function should ensure frontmatter exists and is properly formatted, preserving existing functionality.</action>
  <verify>python -c "from frontmatter_utils import ensure_frontmatter; print('Import successful')"</verify>
  <done>File frontmatter_utils.py created with ensure_frontmatter function that handles frontmatter creation and validation</done>
</task>

<task type="auto">
  <name>Task 2: Update chain_drafter.py to use frontmatter_utils</name>
  <files>chain_drafter.py</files>
  <action>Replace all instances of _ensure_frontmatter and _ensure_frontmatter_closer calls in chain_drafter.py with calls to frontmatter_utils.ensure_frontmatter. Remove the duplicate function definitions.</action>
  <verify>python -c "import chain_drafter; print('Import successful')"</verify>
  <done>chain_drafter.py updated to use frontmatter_utils.ensure_frontmatter with no duplicate frontmatter handling functions</done>
</task>

<task type="auto">
  <name>Task 3: Update chain_publisher_core.py to use frontmatter_utils</name>
  <files>chain_publisher_core.py</files>
  <action>Replace all instances of _ensure_frontmatter and _ensure_frontmatter_closer calls in chain_publisher_core.py with calls to frontmatter_utils.ensure_frontmatter. Remove the duplicate function definitions.</action>
  <verify>python -c "import chain_publisher_core; print('Import successful')"</verify>
  <done>chain_publisher_core.py updated to use frontmatter_utils.ensure_frontmatter with no duplicate frontmatter handling functions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input→function | User-provided text and metadata passed to ensure_frontmatter |
| module→module | Internal modules importing frontmatter_utils |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | frontmatter_utils.py | mitigate | Validate input metadata to prevent injection of harmful frontmatter content |
| T-26-02 | Denial of Service | frontmatter_utils.py | mitigate | Add input size limits to prevent excessive memory usage |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-01/01-01-SUMMARY.md when done
</output>