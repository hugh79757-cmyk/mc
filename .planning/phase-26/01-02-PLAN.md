---
phase: 26
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - chain_card_injector.py
  - chain_drafter.py
  - chain_publisher_core.py
  - image/search_providers.py
  - image/prompt_builder.py
files_created:
  - url_utils.py
autonomous: true
requirements:
  - R-REF-04
  - R-REF-05
user_setup: []

must_haves:
  truths:
    - "URL handling works correctly for all link extraction and normalization"
    - "Tracking parameters are properly stripped from URLs"
    - "Domain extraction functions correctly for all URL formats"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "url_utils.py"
      provides: "Centralized URL handling with domain extraction, normalization, and tracking parameter removal"
      min_lines: 20
    - path: "chain_card_injector.py"
      provides: "Updated to use url_utils for link processing"
      exports: ["extract_domain", "normalize_url", "strip_tracking_params"]
    - path: "chain_drafter.py"
      provides: "Updated to use url_utils for URL processing"
      exports: ["extract_domain"]
    - path: "chain_publisher_core.py"
      provides: "Updated to use url_utils for URL processing"
      exports: ["extract_domain"]
    - path: "image/search_providers.py"
      provides: "Updated to use url_utils for URL processing"
      exports: ["normalize_url"]
    - path: "image/prompt_builder.py"
      provides: "Updated to use url_utils for URL processing"
      exports: ["normalize_url"]
  key_links:
    - from: "chain_card_injector.py"
      to: "url_utils.py"
      via: "import url_utils"
      pattern: "from url_utils import extract_domain, normalize_url, strip_tracking_params"
    - from: "chain_drafter.py"
      to: "url_utils.py"
      via: "import url_utils"
      pattern: "from url_utils import extract_domain"
    - from: "chain_publisher_core.py"
      to: "url_utils.py"
      via: "import url_utils"
      pattern: "from url_utils import extract_domain"
    - from: "image/search_providers.py"
      to: "url_utils.py"
      via: "import url_utils"
      pattern: "from url_utils import normalize_url"
- from: "image/prompt_builder.py"
       to: "url_utils.py"
       via: "import url_utils"
       pattern: "from url_utils import normalize_url"

<objective>
Create URL utility module and update all modules to use it.

Purpose: Extract duplicate URL handling code into a dedicated url_utils module and replace duplicate logic in all modules
Output: url_utils.py module and updated imports in chain_card_injector.py, chain_drafter.py, chain_publisher_core.py, image/search_providers.py, image/prompt_builder.py
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Create url_utils.py with URL handling functions</name>
   <files>url_utils.py</files>
   <action>Create url_utils.py with functions for domain extraction (extract_domain), URL normalization (normalize_url), and tracking parameter removal (strip_tracking_params). Include any other URL handling utilities needed.</action>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create url_utils.py with URL handling functions</name>
  <files>url_utils.py</files>
  <action>Create url_utils.py with functions for domain extraction (extract_domain), URL normalization (normalize_url), and tracking parameter removal (strip_tracking_params). Include any other URL handling utilities needed.</action>
  <verify>python -c "from url_utils import extract_domain, normalize_url, strip_tracking_params; print('Import successful')"</verify>
  <done>File url_utils.py created with URL handling functions</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for url_utils</name>
  <files>test_url_utils.py</files>
  <action>Create unit tests for url_utils functions covering various URL formats, edge cases, and error conditions.</action>
  <verify>python -m pytest test_url_utils.py -v</verify>
  <done>Unit tests for url_utils created and passing</done>
</task>

<task type="auto">
  <name>Task 3: Update modules to use url_utils</name>
  <files>chain_card_injector.py chain_drafter.py chain_publisher_core.py image/search_providers.py image/prompt_builder.py</files>
  <action>Replace all duplicate URL handling code in the specified modules with calls to url_utils functions. Specifically replace domain extraction, normalization, and tracking parameter removal logic with imports from url_utils.</action>
  <verify>python -c "
import chain_card_injector
import chain_drafter
import chain_publisher_core
from image.search_providers import UnsplashProvider, PexelsProvider
from image.prompt_builder import PromptBuilder
print('All modules imported successfully')
# Quick test that functions are accessible
from url_utils import extract_domain, normalize_url, strip_tracking_params
assert extract_domain('http://example.com/path') == 'example.com'
assert normalize_url('http://example.com/path?utm_source=test') == 'http://example.com/path'
assert strip_tracking_params('http://example.com/path?utm_source=test&utm_medium=test') == 'http://example.com/path'
print('URL utility functions work correctly')
"</verify>
  <done>Modules updated to use url_utils for URL processing with no duplicate URL handling code</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input→function | URL strings passed to URL utility functions |
| module→module | Internal modules importing url_utils |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Information Disclosure | url_utils.py | mitigate | Validate URL inputs to prevent information leakage through error messages |
| T-26-05 | Denial of Service | url_utils.py | mitigate | Add timeout and size limits to prevent ReDoS attacks through malicious URLs |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-02/01-02-SUMMARY.md when done
</output>
</output> 07-07-PLAN.md