---
phase: 26
plan: 02
type: execute
wave: 4
depends_on: [03-01]
files_modified:
  - image/search_providers.py
  - image/prompt_builder.py
files_created:
  - 
autonomous: true
requirements:
  - R-REF-12
user_setup: []

must_haves:
  truths:
    - "Image providers correctly inherit from BaseImageProvider"
    - "Image providers use the shared CacheManager for caching"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "image/search_providers.py"
      provides: "Updated UnsplashProvider and PexelsProvider classes that inherit from BaseImageProvider and use CacheManager"
      exports: ["UnsplashProvider", "PexelsProvider"]
    - path: "image/prompt_builder.py"
      provides: "Updated to use BaseImageProvider and CacheManager"
      exports: ["PromptBuilder"]
  key_links:
    - from: "image/search_providers.py"
      to: "image/base_provider.py"
      via: "import base_provider"
      pattern: "from .base_provider import BaseImageProvider"
    - from: "image/search_providers.py"
      to: "image/cache_manager.py"
      via: "import cache_manager"
      pattern: "from .cache_manager import CacheManager"
    - from: "image/prompt_builder.py"
      to: "image/base_provider.py"
      via: "import base_provider"
      pattern: "from .base_provider import BaseImageProvider"
    - from: "image/prompt_builder.py"
      to: "image/cache_manager.py"
      via: "import cache_manager"
      pattern: "from .cache_manager import CacheManager"

<objective>
Refactor image providers to use the base class and shared cache manager.

Purpose: Eliminate duplication in image providers by having them inherit from BaseImageProvider and use the centralized CacheManager
Output: Updated image/providers.py and image/prompt_builder.py files
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor image/search_providers.py to use base class and cache</name>
  <files>image/search_providers.py</files>
  <action>Update UnsplashProvider and PexelsProvider classes to inherit from BaseImageProvider. Implement the required fetch and validate methods. Replace any private caching logic with calls to the shared CacheManager instance.</action>
  <verify>python -c "from image.search_providers import UnsplashProvider, PexelsProvider; up = UnsplashProvider(); pp = PexelsProvider(); print('Providers instantiated')"</verify>
  <done>image/search_providers.py updated with providers inheriting from BaseImageProvider and using CacheManager</done>
</task>

<task type="auto">
  <name>Task 2: Refactor image/prompt_builder.py to use base class and cache</name>
  <files>image/prompt_builder.py</files>
  <action>Update PromptBuilder class to use BaseImageProvider and CacheManager. Replace any direct provider instantiation with the base class interface and use the shared cache for provider instances.</action>
  <verify>python -c "from image.prompt_builder import PromptBuilder; pb = PromptBuilder(); print('PromptBuilder created')"</verify>
  <done>image/prompt_builder.py updated to use BaseImageProvider and CacheManager</done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for refactored image providers</name>
  <files>test_image_providers_refactored.py</files>
  <action>Create unit tests to verify that the refactored providers work correctly, including testing the inheritance from BaseImageProvider and usage of CacheManager.</action>
  <verify>python -m pytest test_image_providers_reduced.py -v</verify>
  <done>Unit tests for refactored image providers created and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider→network | Image providers making external API calls |
| cache→memory | Shared cache storage and retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-22 | Information Disclosure | image/search_providers.py | mitigate | Validate API responses to prevent data leakage |
| T-26-23 | Denial of Service | image/prompt_builder.py | mitigate | Add rate limiting to prevent excessive API calls |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-09/03-02-SUMMARY.md when done
</output>