---
phase: 26
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - chain_card_injector.py
files_created:
  - 
autonomous: true
requirements:
  - R-REF-09
user_setup: []

must_haves:
  truths:
    - "Card injector correctly uses CardGenerator and HtmlRenderer to produce cards"
    - "Card output remains identical to pre-refactor version for all input texts"
    - "Backward-compatible wrapper maintains existing interface"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "chain_card_injector.py"
      provides: "Updated to use CardGenerator and HtmlRenderer for card generation"
      exports: ["inject_related_links", "insert_related_links"]
  key_links:
    - from: "chain_card_injector.py"
      to: "card_generator.py"
      via: "import card_generator"
      pattern: "from card_generator import CardGenerator"
    - from: "chain_card_injector.py"
      to: "html_renderer.py"
      via: "import html_renderer"
      pattern: "from html_renderer import HtmlRenderer"

<objective>
Integrate CardGenerator and HtmlRenderer into the chain card injector.

Purpose: Replace the internal card generation logic in chain_card_injector.py with calls to the new CardGenerator and HtmlRenderer classes
Output: Updated chain_card_injector.py that uses the new component classes
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor chain_card_injector.py to use CardGenerator and HtmlRenderer</name>
  <files>chain_card_injector.py</files>
  <action>Update chain_card_injector.py to instantiate CardGenerator and HtmlRenderer and use them in the inject_* methods. Keep the existing function signatures for backward compatibility but delegate to the new components.</action>
  <verify>python -c "import chain_card_injector; cci = chain_card_injector.ChainCardInjector(); print('Injector created')"</verify>
  <done>chain_card_injector.py updated to use CardGenerator and HtmlRenderer with backward-compatible interface</done>
</task>

<task type="auto">
  <name>Task 2: Run integration tests to verify card output matches</name>
  <files>test_card_integration.py</files>
  <action>Create and run integration tests that compare the output of the refactored chain_card_injector with the original implementation for a variety of inputs to ensure behavioral equivalence.</action>
  <verify>python -m pytest test_card_integration.py -v</verify>
  <done>Integration tests created and passing, confirming output matches pre-refactor version</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component→component | Interaction between CardGenerator, HtmlRenderer, and chain_card_injector |
| input→output | Text input to card output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-14 | Information Disclosure | chain_card_injector.py | mitigate | Ensure generated cards don't leak sensitive information |
| T-26-15 | Tampering | chain_card_injector.py | mitigate | Validate that the refactored code doesn't introduce security vulnerabilities |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-07/02-03-SUMMARY.md when done
</output>