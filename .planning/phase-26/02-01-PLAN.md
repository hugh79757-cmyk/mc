---
phase: 26
plan: 01
type: execute
wave: 2
depends_on: [01-04]
files_created:
  - link_finder.py
autonomous: true
requirements:
  - R-REF-07
user_setup: []

must_haves:
  truths:
    - "LinkFinder correctly extracts links from various text formats"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "link_finder.py"
      provides: "LinkFinder class with find_links method that extracts URLs from text"
      min_lines: 20
  key_links:
    - from: "link_finder.py"
      to: "chain_card_injector.py"
      via: "will be imported by"
      pattern: "from link_finder import LinkFinder"

<objective>
Create LinkFinder class to extract links from text.

Purpose: Replace duplicate link extraction logic with a dedicated LinkFinder class
Output: link_finder.py module
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LinkFinder class with find_links method</name>
  <files>link_finder.py</files>
  <action>Create link_finder.py with a LinkFinder class that has a find_links(text) method returning a list of dictionaries with link information (url, text, position, etc.). The method should extract URLs from text using regex or other appropriate methods.</action>
  <verify>python -c "from link_finder import LinkFinder; lf = LinkFinder(); links = lf.find_links('Test http://example.com'); assert len(links) == 1; assert links[0]['url'] == 'http://example.com'; print('LinkFinder works correctly')"</verify>
  <done>File link_finder.py created with LinkFinder class and find_links method</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for LinkFinder</name>
  <files>test_link_finder.py</files>
  <action>Create unit tests for LinkFinder class covering various URL formats, edge cases, and error conditions. Ensure tests achieve good coverage.</action>
  <verify>python -m pytest test_link_finder.py -v</verify>
  <done>Unit tests for LinkFinder created and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input→method | Text string passed to find_links method |
| module→module | Internal modules importing link_finder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-10 | Information Disclosure | link_finder.py | mitigate | Validate input text length to prevent memory issues |
| T-26-11 | Denial of Service | link_finder.py | mitigate | Limit regex complexity to prevent ReDoS |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-06/02-01-SUMMARY.md when done
</output>