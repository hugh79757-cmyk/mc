---
phase: 26
plan: 04
type: execute
wave: 1
depends_on: [01-03]
files_modified:
  - chain_drafter.py
  - chain_publisher_core.py
  - leak_defense.py
files_created: [] 
autonomous: true
requirements:
  - R-REF-06
  - R-REF-07
  - R-REF-08
user_setup: []

must_haves:
  truths:
    - "Constants are correctly imported and used throughout the codebase"
    - "No hardcoded domain lists or regex patterns remain in source files"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "chain_drafter.py"
      provides: "Updated to use constants from constants module"
      exports: ["AUTHORITY_DOMAINS", "SKIP_DOMAINS", "URL_PATTERN"]
    - path: "chain_publisher_core.py"
      provides: "Updated to use constants from constants module"
      exports: ["AUTHORITY_DOMAINS", "SKIP_DOMAINS"]
    - path: "leak_defense.py"
      provides: "Updated to import leak patterns from constants module"
      exports: ["LEAK_PATTERNS", "LEAK_REGEX"]
  key_links:
    - from: "chain_drafter.py"
      to: "constants.py"
      via: "import constants"
      pattern: "from constants import AUTHORITY_DOMAINS, SKIP_DOMAINS"
    - from: "chain_publisher_core.py"
      to: "constants.py"
      via: "import constants"
      pattern: "from constants import AUTHORITY_DOMAINS, SKIP_DOMAINS"
    - from: "leak_defense.py"
      to: "constants.py"
      via: "import constants"
      pattern: "from constants import LEAK_PATTERNS"

<objective>
Update modules to use the new constants module and remove duplicate constant definitions.

Purpose: Replace hardcoded constants in chain_drafter.py, chain_publisher_core.py, and leak_defense.py with imports from constants.py
Output: Updated modules that import and use constants from constants module
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update chain_drafter.py to use constants</name>
  <files>chain_drafter.py</files>
  <action>Replace all instances of hardcoded AUTHORITY_DOMAINS, SKIP_DOMAINS, and regex patterns with imports from constants.py. Remove the duplicate constant definitions.</action>
  <verify>python -c "import chain_drafter; print('Import successful')"</verify>
  <done>chain_drafter.py updated to use constants module with no duplicate constant definitions</done>
</task>

<task type="auto">
  <name>Task 2: Update chain_publisher_core.py to use constants</name>
  <files>chain_publisher_core.py</files>
  <action>Replace all instances of hardcoded AUTHORITY_DOMAINS and SKIP_DOMAINS with imports from constants.py. Remove the duplicate constant definitions.</action>
  <verify>python -c "import chain_publisher_core; print('Import successful')"</verify>
  <done>chain_publisher_core.py updated to use constants module with no duplicate constant definitions</done>
</task>

<task type="auto">
  <name>Task 3: Update leak_defense.py to use constants</name>
  <files>leak_defense.py</files>
  <action>Replace the hardcoded LEAK_PATTERNS list with an import from constants.py. Remove the duplicate definition.</action>
  <verify>python -c "from leak_defense import LEAK_PATTERNS; print('Import successful')"</verify>
  <done>leak_defense.py updated to use constants module for leak patterns with no duplicate definitions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module→constant | Application accessing constant values |
| constant→module | Constants module providing values to consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06 | Information Disclosure | constants.py | mitigate | Ensure constants don't contain sensitive information like API keys |
| T-26-07 | Tampering | constants.py | mitigate | Consider adding integrity checks for constant values in security-sensitive contexts |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-05/01-05-SUMMARY.md when done
</output>