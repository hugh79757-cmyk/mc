---
phase: 26
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - chain_publisher_core.py
files_created:
  - markdown_processor.py
autonomous: true
requirements:
  - R-REF-15
  - R-REF-16
user_setup: []

must_haves:
  truths:
    - "Markdown processing correctly handles leak protection, symbol cleaning, and table protection"
    - "Frontmatter handling is properly separated from markdown processing"
    - "All existing tests (251) still pass"
    - "Generated markdown output matches pre-refactor version"
  artifacts:
    - path: "markdown_processor.py"
      provides: "MarkdownProcessor class handling leak protection, symbol cleaning, and table protection"
      min_lines: 40
    - path: "chain_publisher_core.py"
      provides: "Updated to use MarkdownProcessor for markdown processing"
      exports: ["MarkdownProcessor"]
  key_links:
    - from: "chain_publisher_core.py"
      to: "markdown_processor.py"
      via: "import markdown_processor"
      pattern: "from markdown_processor import MarkdownProcessor"

<objective>
Refactor markdown processing pipeline into a dedicated MarkdownProcessor class.

Purpose: Separate markdown processing logic (leak protection, symbol cleaning, table protection) from Hugo publishing logic
Output: markdown_processor.py module and updated chain_publisher_core.py
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create markdown_processor.py with processing functions</name>
  <files>markdown_processor.py</files>
  <action>Create markdown_processor.py with a MarkdownProcessor class that encapsulates leak protection (_strip_prompt_leak), symbol cleaning, and table protection (_protect_table_lines) functionality. The class should have a process method that applies all necessary transformations to markdown text.</action>
  <verify>python -c "from markdown_processor import MarkdownProcessor; mp = MarkdownProcessor(); print('MarkdownProcessor created')"</verify>
  <done>File markdown_processor.py created with MarkdownProcessor class</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input→markdown | Raw markdown text entering the processing pipeline |
| processed→output | Processed markdown text ready for Hugo frontmatter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-26 | Information Disclosure | markdown_processor.py | mitigate | Ensure processed markdown doesn't inadvertently leak sensitive information |
| T-26-27 | Denial of Service | markdown_processor.py | mitigate | Limit input size and regex complexity to prevent ReDoS |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-11/03-04-SUMMARY.md when done
</output>