---
phase: 26
plan: 01
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - 
files_created:
  - image/base_provider.py
  - image/cache_manager.py
autonomous: true
requirements:
  - R-REF-10
  - R-REF-11
user_setup: []

must_haves:
  truths:
    - "Image providers share a common base class with fetch and validate methods"
    - "Caching logic is centralized and shared across providers"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "image/base_provider.py"
      provides: "Abstract base class defining the interface for image providers"
      min_lines: 20
    - path: "image/cache_manager.py"
      provides: "LRU cache with TTL support for sharing cached results between providers"
      min_lines: 25
  key_links:
    - from: "image/base_provider.py"
      to: "image/cache_manager.py"
      via: "import cache_manager"
      pattern: "from .cache_manager import CacheManager"

<objective>
Create base image provider class and shared cache manager.

Purpose: Extract common functionality from image providers into a base class and create a centralized cache manager
Output: image/base_provider.py and image/cache_manager.py files
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create image/base_provider.py with base provider class</name>
  <files>image/base_provider.py</files>
  <action>Create image/base_provider.py with an abstract BaseImageProvider class that defines the fetch and validate methods that all concrete image providers must implement. Include proper documentation and type hints.</action>
  <verify>python -c "from image.base_provider import BaseImageProvider; print('BaseImageProvider imported')"</verify>
  <done>File image/base_provider.py created with BaseImageProvider abstract class</done>
</task>

<task type="auto">
  <name>Task 2: Create image/cache_manager.py with LRU cache</name>
  <files>image/cache_manager.py</files>
  <action>Create image/cache_manager.py with a CacheManager class that provides LRU caching with TTL support. The cache should be shareable between different provider instances.</action>
  <verify>python -c "from image.cache_manager import CacheManager; cm = CacheManager(); print('CacheManager created')"</verify>
  <done>File image/cache_manager.py created with CacheManager class implementing LRU with TTL</done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for base provider and cache manager</name>
  <files>test_base_provider.py test_cache_manager.py</files>
  <action>Create unit tests for BaseImageProvider (testing abstract behavior) and CacheManager (testing LRU and TTL functionality).</action>
  <verify>python -m pytest test_base_provider.py test_cache_manager.py -v</verify>
  <done>Unit tests for base provider and cache manager created and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider→cache | Image providers accessing the shared cache |
| cache→memory | In-memory cache storage and retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-20 | Information Disclosure | image/cache_manager.py | mitigate | Ensure cache doesn't store sensitive information and is cleared appropriately |
| T-26-21 | Denial of Service | image/base_provider.py | mitigate | Add timeouts to fetch methods to prevent hanging connections |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-08/03-01-SUMMARY.md when done
</output>