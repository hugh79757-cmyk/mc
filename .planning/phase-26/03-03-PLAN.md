---
phase: 26
plan: 03
type: execute
wave: 4
depends_on: [03-02]
files_modified:
  - chain_publisher_core.py
  - config/prompts.yaml
  - config/keyword_mapping.yaml
files_created:
  - config/schema.yaml
autonomous: true
requirements:
  - R-REF-13
  - R-REF-14
user_setup: []

must_haves:
  truths:
    - "Configuration files validate successfully against the JSON-Schema"
    - "Invalid configuration is caught at load time with clear error messages"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "config/schema.yaml"
      provides: "JSON-Schema definition for validating prompts.yaml and keyword_mapping.yaml"
      min_lines: 30
    - path: "chain_publisher_core.py"
      provides: "Updated to validate configuration against schema at load time"
      exports: ["load_and_validate_config"]
  key_links:
    - from: "chain_publisher_core.py"
      to: "config/schema.yaml"
      via: "jsonschema validation"
      pattern: "jsonschema.validate(config, schema)"

<objective>
Create JSON-Schema for configuration validation and add validation hook.

Purpose: Define JSON-Schema for configuration files and add validation during configuration loading to catch errors early
Output: config/schema.yaml and updated config loading in chain_publisher_core.py
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config/schema.yaml with JSON-Schema definitions</name>
  <files>config/schema.yaml</files>
  <action>Create config/schema.yaml containing JSON-Schema definitions for the structure of prompts.yaml and keyword_mapping.yaml. Include required fields, data types, constraints, and examples.</action>
  <verify>python -c "import yaml, jsonschema; print('Schema validation libraries available')"</verify>
  <done>File config/schema.yaml created with JSON-Schema for configuration validation</done>
</task>

<task type="auto">
  <name>Task 2: Update chain_publisher_core.py to validate configuration</name>
  <files>chain_publisher_core.py</files>
  <action>Modify the configuration loading code in chain_publisher_core.py to validate loaded configuration against the JSON-Schema in config/schema.yaml. Raise a clear error message if validation fails.</action>
  <verify>python -c "import chain_publisher_core; config = chain_publisher_core.load_config(); print('Config loaded and validated')"</verify>
  <done>chain_publisher_core.py updated to validate configuration against JSON-Schema at load time</done>
</task>

<task type="auto">
  <name>Task 3: Test validation with valid and invalid configuration</name>
  <files>test_config_validation.py</files>
  <action>Create test cases to verify that valid configuration passes validation and various forms of invalid configuration are properly rejected with appropriate error messages.</action>
  <verify>python -m pytest test_config_validation.py -v</verify>
  <done>Validation tests created and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file→schema | Configuration file being validated against JSON-Schema |
| schema→application | Validation logic preventing invalid configuration from being used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-24 | Information Disclosure | config/schema.yaml | mitigate | Ensure schema doesn't reveal sensitive information about expected values |
| T-26-25 | Denial of Service | chain_publisher_core.py | mitigate | Limit the size of configuration files that can be processed |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verison>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-10/03-03-SUMMARY.md when done
</output>