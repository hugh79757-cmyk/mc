---
phase: 26
plan: 03
type: execute
wave: 1
depends_on: [01-02]
files_modified:
  - chain_drafter.py
  - chain_publisher_core.py
  - leak_defense.py
files_created:
  - constants.py
autonomous: true
requirements:
  - R-REF-06
user_setup: []

must_haves:
  truths:
    - "Constants are correctly imported and used throughout the codebase"
    - "No hardcoded domain lists or regex patterns remain in source files"
    - "All existing tests (251) still pass"
  artifacts:
    - path: "constants.py"
      provides: "Centralized constants for authority domains, skip domains, and regex patterns"
      min_lines: 25
    - path: "chain_drafter.py"
      provides: "Updated to use constants from constants module"
      exports: ["AUTHORITY_DOMAINS", "SKIP_DOMAINS", "URL_PATTERN"]
    - path: "chain_publisher_core.py"
      provides: "Updated to use constants from constants module"
      exports: ["AUTHORITY_DOMAINS", "SKIP_DOMAINS"]
    - path: "leak_defense.py"
      provides: "Updated to import leak patterns from constants module"
      exports: ["LEAK_PATTERNS", "LEAK_REGEX"]
  key_links:
    - from: "chain_drafter.py"
      to: "constants.py"
      via: "import constants"
      pattern: "from constants import AUTHORITY_DOMAINS, SKIP_DOMAINS"
    - from: "chain_publisher_core.py"
      to: "constants.py"
      via: "import constants"
      pattern: "from constants import AUTHORITY_DOMAINS, SKIP_DOMAINS"
    - from: "leak_defense.py"
      to: "constants.py"
      via: "import constants"
      pattern: "from constants import LEAK_PATTERNS"

<objective>
Create constants module and update dependent modules to use it.

Purpose: Replace duplicate AUTHORITY_DOMAINS, SKIP_DOMAINS, and regex pattern definitions with a single constants module and update all modules that previously defined these constants.
Output: constants.py file and updated chain_drafter.py, chain_publisher_core.py, leak_defense.py
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-26/CONTEXT.md
@.planning/phase-26/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants.py with centralized constants</name>
  <files>constants.py</files>
  <action>Create constants.py containing all constants previously defined in multiple files: AUTHORITY_DOMAINS, SKIP_DOMAINS, and regex patterns for URL detection, email matching, etc. Include documentation for each constant.</action>
  <verify>python -c "from constants import AUTHORITY_DOMAINS, SKIP_DOMAINS; print('Import successful')"</verify>
  <done>File constants.py created with all centralized constants</done>
</task>

<task type="auto">
  <name>Task 2: Update chain_drafter.py, chain_publisher_core.py, and leak_defense.py to use constants</name>
  <files>chain_drafter.py chain_publisher_core.py leak_defense.py</files>
  <action>Replace all instances of hardcoded AUTHORITY_DOMAINS, SKIP_DOMAINS, and regex patterns with imports from constants.py. Remove the duplicate constant definitions.</action>
  <verify>python -c "
import chain_drafter
import chain_publisher_core
from leak_defense import LEAK_PATTERNS
from constants import AUTHORITY_DOMAINS, SKIP_DOMAINS, LEAK_PATTERNS as CONST_LEAK_PATTERNS
# Check that the constants are imported correctly
assert hasattr(chain_drafter, 'AUTHORITY_DOMAINS')
assert hasattr(chain_drafter, 'SKIP_DOMAINS')
assert hasattr(chain_publisher_core, 'AUTHORITY_DOMAINS')
assert hasattr(chain_publisher_core, 'SKIP_DOMAINS')
assert LEAK_PATTERNS == CONST_LEAK_PATTERNS
print('Modules updated successfully and constants match')
"</verify>
  <done>chain_drafter.py, chain_publisher_core.py, and leak_defense.py updated to use constants module with no duplicate constant definitions</done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for constants and verify the updates</name>
  <files>test_constants.py test_chain_drafter_constants.py test_chain_publisher_core_constants.py test_leak_defense_constants.py</files>
  <action>Create unit tests for constants.py to verify that all expected constants are present and have correct values. Also create tests to verify that the updated modules use the constants correctly.</action>
  <verify>python -m pytest test_constants.py test_chain_drafter_constants.py test_chain_publisher_core_constants.py test_leak_defense_constants.py -v</verify>
  <done>Unit tests for constants and updated modules created and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module→constant | Application accessing constant values |
| constant→module | Constants module providing values to consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06 | Information Disclosure | constants.py | mitigate | Ensure constants don't contain sensitive information like API keys |
| T-26-07 | Tampering | constants.py | mitigate | Consider adding integrity checks for constant values in security-sensitive contexts |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
[Overall phase checks]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
Create .planning/phases/26-04/01-04-SUMMARY.md when done
</output>